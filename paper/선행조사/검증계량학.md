# 선행조사 -- 검증 계량학 (Phase 1 종료판)

**이 문서의 1판은 주장을 과하게 잡았다.** 아래는 못 본 칸 여덟 개를 닫고 고친 것이다.
고친 이유와 무엇이 틀렸는지를 지우지 않고 남긴다.

**읽은 수준**: 전문을 읽은 것은 없다. `[출처:조각]` 검색 요약, `[출처:목록]` 제목만.

---

## 1판에서 틀린 것 -- 지우지 않고 적는다

> (1판) "설계 검증에는 이 수가 없습니다."

**틀렸다.** 미국특허 **US7865849B2** (2008, TI) *System and method for estimating test
escapes in integrated circuits* 가 이미, 기존 Williams-Brown 이 **여러 시험을 독립적으로밖에
합성하지 못하는 문제를 지적하고**, 생산 실패 데이터와 고장커버리지를 결합해 **중복
검출(overlap)을 고려한 combined coverage 와 test escape rate** 를 추정한다.
그러니 **"여러 검증층을 합쳐 잔여 결함을 수치화한다" 는 그 자체로 신규성이 아니다.**

1판은 이 특허를 "제목만 봤다" 고 적어 두고도 그 위에 주장을 세웠다. 못 본 것을 적는
것만으로는 부족하다 -- **못 본 칸이 주장의 밑동이면 주장을 세우지 말아야 한다.**

---

## Phase 1 -- 네 질문으로 닫는다

| 질문 | 확인할 것 | 결과 |
|---|---|---|
| Multi-layer composition | 이종 검증을 확률적으로 합성했는가 | **이미 있다** (US7865849 · capture-recapture · 보수적 베이즈) |
| Dependency | 층간 상관/CCF 를 실제 데이터로 추정했는가 | **이미 있다** (상관 capture-recapture · 원자력 PRADIC · possibly-correlated tests) |
| LOD | 고장 크기/등급별 검출확률을 보고했는가 | **찾지 못했다** (한 번의 검색으로. 아래 단서) |
| Certificate | 위를 실제 보증 증거로 묶었는가 | 부분적 (assurance case · ED-324). 거부 의미론은 못 찾음 |

### 7축 대조표

`Cov` 커버리지 · `Res` 잔여위험 · `ML` 다층합성 · `Dep` 층간의존 실측 ·
`LOD` 고장등급별 검출한계 · `Unc` 불확도 · `Cert` 기계발급 증명서

| 선행 | Cov | Res | ML | Dep | LOD | Unc | Cert |
|---|:-:|:-:|:-:|:-:|:-:|:-:|:-:|
| Williams-Brown, *Defect Level as a Function of Fault Coverage* | O | O | X | X | X | X | X |
| **US7865849B2** (TI 2008) test escape, overlapping coverage | O | O | **O** | **O** | X | ? | X |
| Certitude / functional qualification (Certess 2006 → Synopsys) | O | △ | X | X | X | X | X |
| IEEE 6176448 *probabilistic analysis ... under Mutation Analysis* | O | X | X | X | X | X | X |
| capture-recapture (SW inspection, 10년 회고 2004) | X | O | **O** | **O** | X | O | X |
| Good-Turing 상한 arXiv:2607.17061 (2026-07) | X | O | X | △ | X | O | X |
| **보수적 베이즈** (Littlewood·Rushby·Bishop·Povyakalo·Zhao) | X | O | **O** | **O** | X | **O** | △ |
| 원자력 **PRADIC** (INL, RESS 2022) SW CCF 정량화 | X | O | O | **O** | X | O | △ |
| ED-324/ARP6983 · AMLAS · UL 4600 | X | △ | X | X | X | X | O |

**`LOD` 열이 비어 있다.** `Cert` 는 있으나 기계 발급·거부 의미론 쪽은 비어 있다.

---

## 가장 가까운 선행연구 -- 보수적 베이즈 학파 (1판에 없었다. 여기가 제일 가깝다)

City University London 계열(Littlewood, Povyakalo, Strigini, Bishop, Bloomfield, Zhao)이
**"서로 다른 증거를 합치되 독립을 가정하지 않는다"** 를 15년 해 왔다 [출처:조각].

- Littlewood & Rushby (2012), Littlewood & Povyakalo (2013): 1oo2 에서 한 채널의
  **완전성(perfection) 확률**을 쓰면 시스템 pfd 에 간단한 **보수적 표현**이 나온다 --
  한 채널의 pfd 와 다른 채널의 pnp 로 [출처:조각]
- Bishop·Bloomfield·Littlewood·Povyakalo·Wright, *Toward a formalism for conservative
  claims about the dependability of software-based systems*, IEEE TSE [출처:조각]
- **arXiv:2208.00462 / TSE 2022** *The Unnecessity of Assuming Statistically Independent
  Tests in Bayesian Software Reliability Assessments* [출처:목록]
- **arXiv:2208.07935** *Demonstrating Software Reliability using Possibly Correlated
  Tests: Insights from a Conservative Bayesian Approach* [출처:목록]
- arXiv:2511.07038 *Conservative Software Reliability Assessments Using Collections of
  Bayesian Inference Problems* [출처:목록]
- *Conservative claims for the probability of perfection ... using operational experience
  of previous similar systems*, RESS 2018 [출처:목록]

**이것이 1판의 ①(이종 층 합성 + 공통원인)을 거의 덮는다.** 상관된 시험, 독립 가정의 불필요,
보수적 상한이 모두 저쪽에 있다.

## 원자력 쪽 -- 사용자 지적대로 여기가 깊다

- **PRADIC** (Idaho National Lab): DI&C 의 **소프트웨어 CCF 를 식별·확률 추정·영향 평가**
  하는 플랫폼. 정성 위험분석 + 정량 신뢰도 + 결과 분석 3단 통합 [출처:조각, RESS 2022 /
  arXiv:2204.03717 / OSTI 1974870]
- NRC 는 2022-02 공개회의로 DI&C CCF 정책을 **위험정보 기반 대안**까지 넓히는 계획을 다뤘다
  [출처:조각]
- IAEA: 서로 다른 HW·SW·알고리즘·개발방법으로 다양성을 확보하되, **독립을 주장해 실패확률을
  곱하려면 I&C 사슬 전체에서 다양성을 입증해야 한다** [사용자 제공 / 출처:조각]
- National Academies: **다른 소프트웨어라는 사실만으로 실패 독립이 증명되지 않는다**
  [사용자 제공]

---

## β 라는 이름을 쓰지 않는다

IEC 61508 의 β-인자는 **하드웨어 CCF 모델의 파라미터**이고 1oo2 구성과 묶여 있으며 값도
구성·평가방식에 따라 달라진다 [출처:조각]. 검증층 사이의 의존을 β 라고 부르면
"왜 검증층 의존이 하드웨어 CCF 와 같은 확률모형을 따른다고 가정했는가" 에 답해야 한다.
**답이 없다.** 그래서 이름과 지위를 바꾼다.

    D_ij = 층 i, j 사이의 **실측된** 의존 (같은 고장 모집단에 대한 공동 검출로 관측)

    관측량:  P(A∩B) · P(A|B) · P(B|A)      -- 세는 것이다. 모형이 아니다
    쓰임:    기존 보수적 추정량의 **입력**
    β 와의 관계: 나중에 **비교**한다. 가정하지 않는다

## 우리가 그것과 다른 점 -- 주장을 이렇게 바꾼다

> 기존 검증 연구는 커버리지 지표, 잔여결함 추정량, 개별 신뢰도 모형을 제공한다. 그러나 그
> 방법들만으로는 **이종 검증층 · 실측된 층간 의존 · 고장등급별 검출한계 · 불확도**를
> 비교 가능한 하나의 잔여 유출량으로 묶은, **경험적으로 교정된 증명서**가 나오지 않는다.

그리고 한 걸음 더 물러선다. **새 추정량을 만들지 않는다.**
보수적 베이즈 추정량이 더 낫다 -- 우리가 만드는 것은 **그 추정량이 요구하는 입력을 실제로
재는 계측기**다. 저쪽은 추정량이 있는데 입력이 전문가 판단이고, 이쪽은 입력을 셀 수 있다.

---

## 남은 신규성 후보 -- 두 개로 줄었다

1. **고장등급별 검출확률 함수와 검출한계** (`LOD` 열이 비어 있다)

       C(f, θ)   f 고장등급/크기, θ 운용조건
       LOD = min{ f : P_detect(f) >= p0 }

   커버리지 99.5% 는 "못 잡은 50개가 어떤 종류였나" 를 말하지 않는다. 등급별로 갈라 보면
   제어흐름 99.8% · CDC 94% · 희소 프로토콜 위반 71% 처럼 **갈린다**(가설이다, 아직 안 쟀다).

2. **기계 발급 증명서 + 거부 의미론**. 안 잰 층은 빨강. 양성대조 실패한 실행은 **무효**.
   자격에 만료일. SST 는 논문의 주인공이 아니라 **증명서의 선행조건**이다.

## 아직 못 지운 가능성

- `LOD` 가 비었다는 것은 **검색 한 번의 결과**다. 나온 것은 고장주입 *공격* 탐지(보안)와
  IACR 2023/1722 *Quantitative Fault Injection Analysis* 뿐이었다. 소프트 오류(SDC) 문헌에
  고장등급별 확률이 있을 가능성이 크다 -- **안 봤다.**
- **상관 capture-recapture** (*Estimating the number of undetected software errors via the
  correlated capture-recapture model*, EJOR 2006) -- 제목만 봤다. 여러 검사자를 "층" 으로
  보면 우리가 말하는 것과 같을 수 있다. **읽어야 한다.**
- Bühlmann 신뢰도이론을 보증 증거에 쓴 사례를 못 찾았다(신용위험·자율차 책임보험 요율에는
  있다: arXiv:2606.17451). 그러나 검색 한 번이다.
- Good-Turing 의 **의존성 과대추정** 후속 지적(Seongmin Lee) [사용자 제공] -- 원문 미열람.
- Certitude 최신판의 다층 합성 여부 -- 제품 문서 등록 필요, 미열람.
- 규격 원문 전부 미열람: DO-254/ED-80, ICH Q2(R2), IEC 61508-6, ISO/IEC Guide 98(GUM),
  IEC 61511-3.
- 변이검사 서베이(Jia & Harman, *Mutation Testing Advances*)는 **변이적합도 점수**와
  기계학습 기반 변이점수 예측까지 확인했으나, "변이점수 → 잔여결함 확률" 합성은 못 찾았다.
  **서베이 본문을 안 읽었다.**

## 찾아본 질의

- `"probabilistic analysis" functional qualification mutation analysis bug escape probability IEEE 2012 Certitude`
- `mutation score reliability estimation survey Jia Harman mutation testing predict residual faults probability`
- `quantitative evaluation common cause failure software digital I&C nuclear framework CCF probability diverse software 2022`
- `Bayesian combination diverse verification evidence formal proof testing conservative claim Littlewood Rushby`
- `detection probability as function of fault class severity "fault injection" dose response limit of detection`
- `Buhlmann credibility theory applied reliability safety assurance evidence sparse field data`
- (1판) Certitude / LOPA / capture-recapture · Good-Turing / Williams-Brown 합성 / ED-324 / 런타임모니터

## 다음 실험 (Phase 2) -- 가장 작은 것

공개 RTL 하나, **두 층만**. 같은 고장 모집단을 두 층에 똑같이 먹인다.

    Layer A = 변이검사(mutation)
    Layer B = 지시/무작위 시험(directed/random)

    기록: fault_id | class | A_detect | B_detect

이 표 하나에서 `P(A∩B)` · `P(A|B)` · `P(B|A)` 가 바로 나온다. **모형 없이 세는 것이다.**
Phase 3 에서 형식검증 또는 고장주입을 세 번째 층으로 붙인다.
**D_ij 를 코드로 짓기 전에, 이 표가 실제로 갈리는지부터 본다.**
