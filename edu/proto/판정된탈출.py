# -*- coding: utf-8 -*-
"""변이 점수에서 **등가 변이**로 판정한 탈출들.

점수를 올리려고 조용히 빼는 것이 아니다 -- 판정과 **이유**를 남긴다.
이유가 틀렸다면 여기서 반박할 수 있다.  억누른 탈출은 그럴 수 없다.

## 이 다섯은 전수로 확인했다

다섯 다 `use_alt7` 식의 OR 를 XOR 로 바꾼 것이다.  그 식의 항들이 서로
**배타적**이라 OR 와 XOR 가 같은 함수가 된다:

  * `lo5` 는 한 값만 가지므로 `(lo5==17)`·`(lo5==18)`·`(lo5==20)` 중
    많아야 하나가 참이다.  배타적인 항들에 대해 `a|b == a^b` 다.
  * 최상위 `|` 의 양쪽은 `~rd_mid & ...` 와 `rd_mid & ...` 라, `rd_mid`
    하나 때문에 동시에 참이 될 수 없다.

주장으로 두지 않고 `lo5` 32 가지 x `rd_mid` 2 가지 = **64 가지 전부**에서
원본과 변이의 출력을 비교했다.  다섯 다 **64/64 에서 같았다.**

그러면 왜 OR 로 썼나 -- XOR 가 같다면 아무거나 써도 되는 것 아닌가?
아니다.  OR 가 **뜻을 말한다**: "이 셋 중 하나라도 해당하면".  XOR 는
"홀수 개가 해당하면" 이라 읽히고, 항이 배타적이라는 사실을 읽는 사람이
따로 증명해야 뜻이 통한다.  같은 회로를 내더라도 **읽는 사람에게 더
적은 증명을 시키는 쪽**을 고른다.
"""

판정 = [
    {"규칙": "OR / XOR", "줄번호": 98, "파일": "enc8b10b.v",
     "이유": "`(lo5==17)|(lo5==18)` 를 `^` 로 바꾼 것. lo5 는 한 값뿐이라 두 항이 배타적이므로 OR 와 XOR 가 같다. lo5 32 x rd_mid 2 = 64 가지 전수 확인, 차이 0."},
    {"규칙": "OR / XOR", "줄번호": 98, "파일": "enc8b10b.v",
     "이유": "`(lo5==18)|(lo5==20)` 를 `^` 로. 위와 같은 이유. 64 가지 전수 확인, 차이 0."},
    {"규칙": "OR / XOR", "줄번호": 98, "파일": "enc8b10b.v",
     "이유": "최상위 `|` 를 `^` 로. 양쪽이 `~rd_mid & ...` 와 `rd_mid & ...` 라 동시에 참이 될 수 없다. 64 가지 전수 확인, 차이 0."},
    {"규칙": "OR / XOR", "줄번호": 99, "파일": "enc8b10b.v",
     "이유": "`(lo5==11)|(lo5==13)` 를 `^` 로. 배타적. 64 가지 전수 확인, 차이 0."},
    {"규칙": "OR / XOR", "줄번호": 99, "파일": "enc8b10b.v",
     "이유": "`(lo5==13)|(lo5==14)` 를 `^` 로. 배타적. 64 가지 전수 확인, 차이 0."},
]
