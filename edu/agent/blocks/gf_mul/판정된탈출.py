# -*- coding: utf-8 -*-
"""변이 점수에서 **등가 변이**로 판정한 탈출들.

점수를 올리려고 조용히 빼는 것이 아니다 -- 판정과 **이유**를 남긴다.
이유가 틀렸다면 여기서 반박할 수 있다. 억누른 탈출은 그럴 수 없다.
"""

판정 = [
    {"규칙": '비교 경계 < / <=', "줄번호": 17, "파일": "gf_mul.v",
     "이유": '루프가 한 번 더 돈다. 그때 b[10] 은 [9:0] 범위 밖이라 x 이고, Verilog 의 `if (x)` 는 거짓이므로 acc 가 안 바뀐다. p 만 한 번 더 시프트되지만 출력에 안 쓰인다. **동작 등가**.'},
]
