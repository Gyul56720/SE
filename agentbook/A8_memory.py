# -*- coding: utf-8 -*-
"""A8 -- 메모리 추상. 창 밖에 무엇을 두고 어떻게 다시 찾나."""
import math
import os
import sys

sys.path.insert(0, os.path.join(os.path.dirname(os.path.dirname(
    os.path.abspath(__file__))), "edu"))
from bookK import 장, 개념, 표, 그림, 정의, 유도, 예제, 짚기, 사고, 수, 쓰는자리  # noqa: E402
import sch  # noqa: E402

저장소 = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))


def 검색성공(재현율, k, 관련문서=3):
    """상위 k 개 안에 관련 문서가 적어도 하나 들어올 확률 (독립 가정)."""
    p = min(1.0, 재현율 * k / max(관련문서, 1) / k * 관련문서)
    return 1 - (1 - min(재현율, 1.0)) ** 관련문서


def 끝까지쓸모(검색성공률, 걸음):
    return 검색성공률 ** 걸음


def 망각(나이_일, 반감기_일):
    return 0.5 ** (나이_일 / max(반감기_일, 1e-9))


def 원장세기():
    총, 파일 = 0, 0
    for 뿌리, _d, fs in os.walk(저장소):
        if ".git" in 뿌리:
            continue
        for f in fs:
            if f.endswith(".jsonl"):
                파일 += 1
                try:
                    with open(os.path.join(뿌리, f), encoding="utf-8",
                              errors="replace") as fh:
                        총 += sum(1 for _ in fh)
                except OSError:
                    pass
    return 파일, 총


def ch_memory():
    파일수, 줄수 = 원장세기()
    c = 장(
        "A8", "메모리 추상 — 창 밖의 것을 다시 찾는 일",
        "메모리는 '저장' 이 아니라 **'다시 찾기'** 다. 넣는 것은 쉽고 "
        "**필요할 때 정확히 그것이 나오게 하는 것**이 전부다.",
        쓰는것=["컨텍스트예산", "선택적주입", "컨텍스트오염", "제어루프", "토큰", "작업기억"],
        내놓는것=["에피소드기억", "의미기억", "검색", "재현율", "정밀도",
              "망각", "원장", "출처표시", "쓰기정책"],
        특허="""<b>무엇을 기억할지 고르는 것</b>과 <b>언제 잊을지 정하는 것</b>이 출원이
        몰리는 자리다. 저장은 오래된 기술이고, 에이전트 특유의 문제는 <b>모형이 만든
        내용을 기억에 넣어도 되는가</b>(환각의 영구화)와 <b>기억이 틀렸을 때 어떻게
        되돌리나</b> 이다.""")

    c.글("""창은 매 걸음 다시 내는 비용이다(A6). 그러면 자연스러운 답은
    "창 밖에 두고 필요할 때 꺼내자" 이고, 그것이 메모리다. 문제는 그 다음이다 &mdash;
    <b>필요할 때 꺼낸다는 것을 누가 판단하나.</b> 대부분의 메모리 시스템은
    저장에서 실패하지 않고 <b>검색에서 실패한다</b>.""")

    # ------------------------------------------------------------------
    c.절("A8.1 세 가지 기억을 섞지 마라")

    c.날것(표("<b>에이전트 메모리는 한 가지가 아니다.</b> 셋을 한 저장소에 섞으면 "
            "검색이 전부 망가진다 &mdash; 성질이 다르기 때문이다.",
            ["갈래", "무엇", "수명", "검색 방식", "쓰기 권한"],
            [["<b>작업 기억</b>", "지금 과제의 중간 상태", "과제 하나",
              "전부 창에 (검색 없음)", "런타임"],
             ["<b>에피소드 기억</b>", "무슨 일이 있었나 (원장·트레이스)",
              "영구 (추가만)", "시간·id 로 조회", "<b>런타임만</b> &mdash; 모형이 못 쓴다"],
             ["<b>의미 기억</b>", "알게 된 사실 · 선호 · 규칙", "영구 (수정 가능)",
              "의미 검색", "모형 제안 + <b>사람/검사 승인</b>"],
             ["<b>절차 기억</b>", "하는 법 (스킬·도구·플레이북)", "판 관리",
              "이름·상황으로", "코드 리뷰를 거친다"]]))

    c.날것(짚기("""<b>에피소드 기억을 모형이 쓰게 하면 안 되는 이유.</b>
    원장은 <b>무슨 일이 있었나</b>의 기록이고, 그것이 신뢰할 수 있는 까닭은
    <b>추가만 되고 고쳐지지 않기</b> 때문이다. 모형이 거기에 쓰게 하면
    그 성질이 깨지고, 그러면 사고를 조사할 때 <b>기록 자체를 못 믿는다</b>.
    이 저장소의 원장이 전부 <code>jsonl</code> 추가 전용인 까닭이 그것이다."""))

    c.날것(사고("""<b>실측(이 저장소, 2026-09-15): 검사가 기억을 더럽힌 사고.</b>
    커밋을 밀려는데 <code>codify/ledger.jsonl</code> 이 main 과 충돌했다.
    파 보니 <b>검사가 낳은 줄 다섯</b>이 <code>git add -A</code> 로 쓸려 들어가 있었다.<br><br>
    그래서 규칙이 생겼다: <b>"검사는 재는 것이지 남기는 것이 아니다."</b>
    기관을 진짜로 돌려 보는 점검은 <code>SE_LEDGER_ROOT</code> 를 세워
    임시 자리에 쓰게 한다. 점검은 '도는가' 만 보면 되고,
    <b>도는 것을 보려고 판정의 역사를 더럽힐 이유가 없다</b>.<br><br>
    한 번 더 배운 것: <b>깃발(<code>--저장소</code>)은 그 파일 하나에만 닿는다.</b>
    속에서 부르는 모듈은 못 받는다 &mdash; <b>환경 변수라야 손자까지 내려간다</b>."""))

    # ------------------------------------------------------------------
    c.절("A8.2 검색이 전부다")

    c.날것(정의("재현율 (recall)",
              "관련 있는 것 중 <b>실제로 찾아낸</b> 비율. 에이전트 메모리에서 "
              "<b>재현율이 정밀도보다 훨씬 중요하다</b> &mdash; 못 찾은 것은 "
              "없는 것과 같지만, 잘못 찾은 것은 모형이 무시할 수 있기 때문이다."))

    c.날것(유도("검색이 루프 전체에 미치는 영향", [
        ("한 걸음에서 필요한 사실을 찾을 확률을 r 이라 하자",
         "P(걸음 성공) &asymp; r"),
        ("n 걸음 동안 매번 필요하면",
         "P(끝까지) = r<sup>n</sup>"),
        ("r = 0.9, n = 20 이면",
         f"{수(끝까지쓸모(0.9, 20), 4)}"),
        ("<b>A1 의 곱셈이 여기서 다시 나온다.</b> 검색 실패는 조용하다 &mdash; "
         "오류가 안 나고 <b>모형이 모르는 채로 계속 간다</b>", ""),
    ]))

    줄 = []
    for r in (0.7, 0.8, 0.9, 0.95, 0.99):
        줄.append([수(r, 3)] + [수(끝까지쓸모(r, n), 4) for n in (5, 10, 20, 50)])
    c.날것(표("검색 성공률 r 과 걸음 수에 대한 <b>끝까지 필요한 것을 다 찾을 확률</b>. "
            "<b>r = 0.9 는 좋아 보이지만 20 걸음이면 절반 아래다.</b>",
            ["검색 성공률 r", "n=5", "n=10", "n=20", "n=50"], 줄))

    c.날것(개념(
        "검색 실패를 들리게 만든다",
        """검색이 못 찾으면 아무 일도 안 일어난다 &mdash; 빈 결과가 돌아오고 모형은
        그냥 모르는 채로 간다. <b>이 조용함이 메모리 시스템의 가장 큰 병이다.</b><br><br>
        고치는 법은 검색을 잘하게 만드는 것이 아니라 <b>못 찾았다는 사실을 신호로
        만드는 것</b>이다: ① 빈 결과를 빈 문자열이 아니라 <b>명시적 '못 찾음'</b>으로
        돌려준다 ② 검색 점수가 임계 아래면 '자신 없음' 을 같이 준다
        ③ 못 찾은 질의를 <b>원장에 남긴다</b> &mdash; 그것이 메모리를 고칠 목록이 된다.""",
        어디에="메모리 검색 인터페이스.",
        언제="처음부터. 나중에 넣으면 그동안 조용히 실패한 것을 모른다.",
        어떻게="반환을 <code>{찾음: [...], 못찾음: True/False, 최고점수: float}</code> 로. "
             "그리고 <b>못 찾은 질의를 세어 대시보드에 올린다</b>(A13).",
        산업코드="""결과 = 메모리.검색(질의, k=5)
if 결과["최고점수"] < 임계:
    원장.적기({"못찾음": 질의, "최고점수": 결과["최고점수"]})
    return "관련된 기억을 못 찾았다. 도구로 직접 확인하라."   # **명시적으로**
# 나쁜 예: return ""  -> 모형은 '기억이 없다' 와 '검색이 고장' 을 구별 못 한다""",
        주의="'자신 없음' 을 모형에게 주면 모형이 그것을 <b>과신의 근거</b>로 쓸 수도 "
           "있다 &mdash; '기억에 없으니 없는 것이다'. 그래서 문구가 중요하다: "
           "<b>'없다' 가 아니라 '못 찾았다'</b>."))

    # ------------------------------------------------------------------
    c.절("A8.3 쓰기 정책 — 무엇을 기억에 넣을 것인가")

    c.날것(표("<b>쓰기 정책이 메모리 품질의 상한이다.</b> 아무거나 넣으면 "
            "검색이 아무리 좋아도 쓰레기가 나온다.",
            ["무엇", "넣나", "왜"],
            [["도구가 관측한 사실", "<b>넣는다</b>", "출처가 있고 재확인 가능하다"],
             ["사람이 준 선호·제약", "<b>넣는다</b>", "다시 물어보면 안 되는 것"],
             ["모형의 중간 결론", "<b>조심</b>", "환각이 영구화된다. 출처를 달고 넣는다"],
             ["모형의 요약", "<b>원본과 함께만</b>", "요약이 틀리면 원본으로 돌아갈 수 있어야"],
             ["실패한 시도", "<b>넣는다</b>", "같은 실패를 반복하지 않게. 대개 안 넣는다"],
             ["원본 전문", "<b>따로</b>", "검색 색인과 원본을 분리 &mdash; 색인은 다시 지을 수 있다"]]))

    c.날것(짚기("""<b>'실패한 시도를 기억한다' 가 저평가되어 있다.</b>
    에이전트는 같은 막다른 길을 반복해서 간다 &mdash; 기억이 성공만 담기 때문이다.
    A4 의 무진전 판정이 걸음 안에서 그것을 막는다면, 실패 기억은 <b>과제를
    넘어서</b> 막는다."""))

    줄2 = []
    for 반감기 in (1, 7, 30, 365):
        줄2.append([f"{반감기}일"] + [수(망각(t, 반감기), 4)
                                  for t in (1, 7, 30, 90, 365)])
    c.날것(표("<b>망각 곡선.</b> 기억마다 반감기를 달리 준다 &mdash; "
            "'지금 작업 중인 브랜치' 는 하루, '이 사람이 한국어를 쓴다' 는 영구. "
            "<b>반감기를 안 정하면 전부 영구가 되고, 그러면 오래된 사실이 "
            "새 사실과 같은 무게로 검색된다</b>.",
            ["반감기", "1일 뒤", "7일 뒤", "30일 뒤", "90일 뒤", "1년 뒤"], 줄2))

    # ------------------------------------------------------------------
    c.절("A8.4 이 저장소의 기억을 세어 본다")

    c.날것(표(f"빌드 시점에 센 것: <code>.jsonl</code> 원장 <b>{파일수}개</b>, "
            f"합계 <b>{줄수:,}줄</b>. 이것이 이 시스템의 <b>에피소드 기억</b>이다 "
            "&mdash; 추가 전용이고, 모형이 못 쓰고, 사고를 조사할 때 유일하게 믿는 것.",
            ["잰 것", "값", "성질"],
            [["원장 파일", f"{파일수}개", "기관마다 하나씩"],
             ["원장 줄", f"{줄수:,}줄", "한 줄이 한 사건"],
             ["쓰기 권한", "런타임만", "모형은 못 쓴다 &mdash; 그래서 믿을 수 있다"],
             ["수정", "없음 (추가만)", "고쳐지면 기록이 아니다"]]))

    c.날것(예제(
        "검색 k 를 몇으로 할 것인가",
        "관련 문서가 평균 3개 있고, 검색기의 문서당 재현율이 0.6 이다. "
        "상위 k 개 안에 <b>적어도 하나</b>가 들어올 확률은?",
        "관련 문서 각각이 독립적으로 잡힌다고 보면 1 − (1−0.6)³.",
        f"<b>{수(검색성공(0.6, 5), 4)}</b> &mdash; 하나만 필요하면 이것으로 충분할 수 있다.",
        "<b>'하나면 충분한가' 가 진짜 질문이다.</b> 위 계산은 관련 문서 셋이 "
        "<b>같은 정보</b>를 담는다고 가정한다. 서로 다른 조각이면 셋 다 필요하고, "
        f"그 확률은 0.6³ = {수(0.6**3, 4)} 로 뚝 떨어진다.",
        덧="그래서 실무에서 k 를 정하기 전에 <b>'관련 문서들이 중복인가 상보인가'</b> 를 "
          "먼저 본다. 중복이면 k 를 작게, 상보면 크게 &mdash; 그리고 상보인 경우에는 "
          "<b>검색보다 구조화</b>가 낫다(문서를 쪼개지 말고 한 덩어리로 두는 것)."))

    c.날것(쓰는자리([
        ["메모리를 설계할 때", "세 갈래를 섞지 마라. 특히 <b>원장은 모형이 못 쓰게</b>"],
        ["'기억을 못 한다' 는 보고를 받을 때", "저장이 아니라 <b>검색</b>을 의심하라"],
        ["검색 인터페이스를 만들 때", "<b>못 찾음을 명시적으로</b> 돌려준다"],
        ["무엇을 기억할지 정할 때", "출처가 없는 것은 넣지 않는다. 실패도 기억한다"],
    ]))
    return c.완성()
