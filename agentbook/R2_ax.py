# -*- coding: utf-8 -*-
"""R2 -- AX (Agent Executor). 에이전트를 **워크로드**로 다루면 무엇이 달라지나."""
import os
import sys

sys.path.insert(0, os.path.join(os.path.dirname(os.path.dirname(
    os.path.abspath(__file__))), "edu"))
sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))
from bookK import 장, 개념, 표, 정의, 유도, 예제, 짚기, 사고, 수  # noqa: E402
from bookA import 정리, 보조정리, 레포, 소스, 말풀이, 직무, 언어  # noqa: E402
import dia  # noqa: E402


def ch_ax():
    c = 장(
        "R2", "AX — 에이전트를 워크로드로 다룬다",
        "LangChain 은 <b>한 프로세스 안</b>의 호출을 엮는다. AX 는 그 위 층이다 — "
        "에이전트 <b>하나하나를 클러스터의 일감</b>으로 보고, 격리·네트워크·재개를 "
        "선언으로 정한다. 쿠버네티스를 써 봤다면 낯익을 것이다.",
        쓰는것=["선언형", "상태기계", "체크포인트", "재개", "제어루프", "고정점",
             "도구", "샌드박스", "일관된인터페이스", "실행id"],
        내놓는것=["조정고리", "원하는상태", "관찰된상태", "이상향수렴", "매니페스트",
              "적용", "멱등한보장", "태스크", "작업공간", "관문", "송출허용목록",
              "중지와재개", "다중테넌시", "제어평면", "자료평면"],
        특허="""AX 가 발명한 것은 <b>에이전트를 쿠버네티스 객체처럼 다루는 사고</b>가
        아니다 &mdash; 조정 고리는 2014년 쿠버네티스이고 그 뿌리는 제어이론이다.
        새로운 자리는 <b>무엇을 선언 가능한 것으로 골랐는가</b>다: 격리(Task) ·
        미리 데운 작업공간(Workspace) · <b>나가는 트래픽의 허용목록</b>(Gateway) ·
        플랫폼이 쓰는 모델(Model). 넷 중 셋은 <b>에이전트가 돈과 비밀을 태울 수 있다</b>는
        사실에서 나왔다.""")

    c.날것(직무(["AIE.런타임", "AIE.인터페이스", "AIE.코어", "NAV.프레임워크", "NAV.관측", "NAV.오픈소스"]))

    # ------------------------------------------------------------------
    c.절("R2.1 말부터")

    c.날것(말풀이([
        ("조정 고리", "reconciliation loop",
         "원하는 상태와 지금 상태를 견주어 차이를 줄이는 쪽으로 한 걸음 옮기는 것을 "
         "되풀이한다. 한 번에 다 맞추려 하지 않는다.",
         "reconcile 은 회계에서 '장부를 맞춘다' 는 말이다. 두 장부(원함·실제)를 "
         "나란히 놓고 차이를 없애는 그림이 같다."),
        ("멱등", "idempotent",
         "여러 번 해도 한 번 한 것과 같다. `EnsureAtespace` 를 열 번 불러도 "
         "공간은 하나다.",
         "idem(같은) + potent(힘). 같은 힘을 몇 번 줘도 결과가 같다는 뜻."),
        ("매니페스트", "manifest",
         "무엇을 원하는지 적은 선언 파일. AX 에서는 `apiVersion/kind/metadata/spec` "
         "네 칸을 가진 YAML 이다.",
         "배가 싣고 가는 짐의 목록(積荷目錄)이 manifest 다. '무엇이 실려 있어야 하나' "
         "를 적은 종이라는 뜻이 그대로 왔다."),
        ("샌드박스", "sandbox",
         "믿을 수 없는 코드를 가둬 놓고 돌리는 방. CPU·메모리 한도와 파일시스템이 "
         "바깥과 갈라져 있다.",
         "아이가 모래밭에서 놀면 모래가 밖으로 안 나간다는 비유. 1970년대 보안 문헌에서 왔다."),
        ("송출 허용목록", "egress allowlist",
         "안에서 **밖으로** 나가는 연결을 열거된 호스트로만 제한한다.",
         "egress 는 라틴어 'e-(밖으로) + gradi(걷다)'. 들어오는 것(ingress)보다 "
         "나가는 것이 위험한 워크로드가 에이전트다 &mdash; 비밀을 들고 나갈 수 있으므로."),
        ("제어 평면 / 자료 평면", "control plane / data plane",
         "무엇을 할지 정하는 층과 실제로 일이 흐르는 층을 가른다. AX 에서 "
         "`ax-controller` 가 앞이고 샌드박스 안의 러너가 뒤다.",
         "통신망 설계에서 온 말. 신호(제어)와 통화(자료)가 다른 선로를 탄다는 그림."),
    ]))

    # ------------------------------------------------------------------
    c.절("R2.2 무엇을 만들었나 — 그리고 왜 이 넷인가")

    c.날것(레포("google/ax", "https://github.com/google/ax", "d8ed0fe",
             [("한 줄", "Google's open agentic orchestration runtime"),
              ("말", "Go (32 파일 · 14,178 줄)"),
              ("깔개", "Agent Substrate (샌드박스 실행)"),
              ("판", "v1alpha1 — <b>깨지는 변경이 온다고 README 가 경고한다</b>"),
              ("읽은 날", "2026-09-22")],
             한마디="이 장의 모든 인용은 이 커밋에서 떴다. "
                  "<b>알파다</b> &mdash; 여기 적힌 이름은 바뀔 수 있다."))

    c.글("""README 의 '왜?' 절이 이 도구의 논거를 한 문단으로 적는다. 옮기면 이렇다 —
    <b>에이전트는 새로운 종류의 일감이다.</b> 무상태 마이크로서비스도 아니고,
    돌다 끝나는 배치 작업도 아니다. 상태를 쌓고, 엄격한 격리가 필요하고, 모델 API 와
    도구 서버를 부르고, <b>아무도 안 보면 고리를 돌며 돈을 태운다.</b>""")

    c.날것(표("네 원함과 네 기본형 — 무엇이 선언 가능한 것으로 뽑혔나",
           ["하고 싶은 것", "AX 가 주는 것", "왜 이것이 선언될 값어치가 있나"],
           [["믿을 수 없는 코드를 CPU·메모리 한도 안에서 격리해 돌린다",
             "<b>Task</b>", "에이전트는 제가 쓴 코드를 제가 돌린다 &mdash; 격리가 선택이 아니다"],
            ["Git·MCP·스킬을 미리 물려 시작부터 데워 둔다",
             "<b>Workspace</b>", "찬 시작은 매 실행마다 같은 왕복을 다시 문다"],
            ["나가는 트래픽을 열거된 호스트로만 잠근다",
             "<b>Gateway</b>", "들어오는 것보다 <b>나가는 것</b>이 위험하다 &mdash; 비밀이 그리로 샌다"],
            ["플랫폼이 쓸 LLM 과 그 열쇠를 정한다",
             "<b>Model</b>", "열쇠가 매니페스트에 없고 쿠버네티스 시크릿을 가리킨다"],
            ["논 에이전트를 멈췄다가 <b>그 자리에서</b> 잇는다",
             "<code>ax suspend/resume</code>", "긴 일은 반드시 중간에 끊긴다"],
            ["도는 에이전트 안으로 들어가 무엇을 하는지 본다",
             "<code>ax ssh</code>", "보이지 않는 것은 검사할 수 없다"]]))

    c.날것(사고("""<b>셋이 돈과 비밀에서 나왔다.</b> Task(격리) · Gateway(송출) ·
    Model(시크릿 참조)은 성능 문제가 아니라 <b>손해 통제</b> 문제다. 에이전트 런타임의
    설계가 서비스 런타임과 갈리는 자리가 여기다 &mdash; 마이크로서비스는 잘못 돌면
    느려지고, 에이전트는 잘못 돌면 <b>청구서가 온다.</b>"""))

    # ------------------------------------------------------------------
    c.절("R2.3 깔아 보기 — 그리고 무엇이 필요한가")

    c.날것(언어("같은 일을 세 말로 — 왜 AX 는 Go 인가", [
        ("Go", 'go install github.com/google/ax/cmd/ax@latest',
         "받아서 <b>정적 링크 바이너리 하나</b>를 놓는다. 돌릴 기계에 Go 가 없어도 된다 — "
         "샌드박스 이미지에 런타임을 안 깔아도 되니 이미지가 작고 시작이 빠르다"),
        ("Python", 'pip install ax   # 그리고 파이썬 3.x + 의존성 트리가 따라온다',
         "샌드박스 이미지마다 인터프리터와 휠을 얹어야 한다. "
         "AX 가 <b>수십억 태스크</b>를 목표로 하면 그 고정비가 곱해진다"),
        ("Rust", 'cargo install ax   # 역시 바이너리 하나',
         "Go 와 같은 이점을 주지만 쿠버네티스 생태계의 클라이언트·프로토 도구가 "
         "Go 쪽에 쌓여 있다 — <b>고른 까닭은 말의 우열이 아니라 이웃</b>이다"),
    ], 짚기="<code>cmd/</code> 아래 한 폴더가 한 실행파일이다. AX 는 넷이다 — "
           "<code>ax</code>(CLI) · <code>ax-server</code> · <code>ax-controller</code>(조정 고리) · "
           "<code>ax-task-runner</code>(샌드박스 안)."))

    c.코드("""# 1. CLI 를 깐다 (Go 1.21+ 필요)
go install github.com/google/ax/cmd/ax@latest

# 2. 클러스터에 제어평면을 올린다 — 이 둘이 서버와 조정 고리다
kubectl apply -f deploy/ax-server.yaml
kubectl apply -f deploy/ax-controller.yaml
kubectl apply -f deploy/redis.yaml        # 상태 보관

# 3. 모델 열쇠는 매니페스트가 아니라 시크릿에 둔다
kubectl create secret generic gemini-api-secret \\
    --from-literal=GEMINI_API_KEY=$GEMINI_API_KEY""")

    c.날것(짚기("""<b>열쇠가 YAML 에 없다.</b> `Model` 은 `secretKey: {name, key}` 로
    <b>가리키기만</b> 한다. 매니페스트는 깃에 들어가는 물건이고, 열쇠는 들어가면 안 되는
    물건이다 &mdash; 이 갈라짐을 타입이 강제한다. 이 저장소도 같은 자리에서 한 번 앓았다
    (`!열쇠` 명령이 생긴 까닭)."""))

    c.코드("""# 4. 일감을 선언하고 적용한다
ax apply -f examples/task.yaml

# 5. 본다
ax get tasks
ax watch task task123              # 단계와 조건이 바뀔 때마다 흘러나온다
ax ssh task123 -- ls -la /workspace   # 샌드박스 안으로 들어간다 (debug: true 일 때만)

# 6. 멈췄다 잇는다
ax suspend task task123            # 체크포인트를 찍고 멈춘다
ax resume  task task123            # 그 자리에서 잇는다""")

    # ------------------------------------------------------------------
    c.절("R2.4 매니페스트를 한 줄씩")

    c.날것(소스("ax_task_manifest",
             "이것이 사람이 쓰는 전부다. 나머지는 클러스터가 한다.",
             풀이=[
                 (16, "apiVersion — <b>판을 글에 박는다.</b> v1alpha1 은 '깨진다' 는 뜻의 "
                      "관용 표시다. 이 줄이 있어서 다음 판이 와도 옛 글이 옛 규칙으로 읽힌다"),
                 (17, "kind — 무엇에 관한 선언인가. 한 파일에 여러 kind 를 `---` 로 "
                      "잇는다(Task + Workspace + Gateway + Model 이 한 파일에)"),
                 (18, "metadata 아래 name — <b>이름이 신원이다.</b> 뒤에서 보겠지만 "
                      "actor 이름이 task 이름과 같게 강제된다"),
                 (20, "atespace — 쿠버네티스의 namespace 에 해당하는 칸막이. "
                      "다중 테넌시의 단위이고, 비면 `default` 로 채워진다"),
                 (22, "spec — <b>원하는 상태.</b> 여기부터가 사람의 뜻이고, "
                      "status(아래 없음)는 클러스터가 채우는 칸이다"),
                 (26, "image — 샌드박스에서 돌 컨테이너. <b>다이제스트로 고정</b>돼 있다 "
                      "(@sha256:…) &mdash; 태그는 움직이고 다이제스트는 안 움직인다"),
                 (28, "resources.requests — 스케줄러에게 '이만큼은 달라'. "
                      "limits 는 '이 이상은 못 쓴다'. 고리를 돌며 메모리를 태우는 "
                      "에이전트가 이웃을 죽이지 못하게 하는 자리다"),
                 (36, "workspaces — 이름으로 Workspace 객체를 가리키고 마운트 경로를 준다. "
                      "<b>참조이지 복사가 아니다</b> &mdash; 같은 작업공간을 여러 태스크가 쓴다"),
                 (39, "gateway — 나가는 트래픽 규칙을 가리킨다. 이것이 없으면 "
                      "에이전트가 아무 데나 연결할 수 있다"),
             ]))

    # ------------------------------------------------------------------
    c.절("R2.5 YAML 을 믿지 않는 법 — 이 책 S1 의 정리가 실제로 풀린 자리")

    c.글("""S1 에서 <b>YAML 의 암묵 변환이 왕복을 깬다</b>는 것을 보았다
    (<code>NO</code> 가 <code>false</code> 가 된다). AX 는 그 문제를 YAML 안에서
    풀지 않는다. <b>진실의 원본을 protobuf 로 옮기고 YAML 을 다리로만 쓴다.</b>""")

    c.날것(소스("ax_yaml_proto",
             "types.go 의 이 주석이 이 저장소의 설계 판단 하나를 통째로 담고 있다.",
             풀이=[
                 (45, "<b>API 종류가 protobuf 메시지다.</b> 그러면 필드 이름·시각 꼴·"
                      "비었을 때 생략 여부가 전부 proto 정의 한 곳에서 나온다"),
                 (48, "YAML 은 <b>일반적으로 먼저 해독</b>된다 — 아무 map 으로 읽는다"),
                 (49, "그 다음 JSON 으로 바꾸고, <b>protojson 이 엄격하게</b> 해독한다"),
                 (50, "<b>모르는 필드와 오타는 거부된다.</b> 이 한 줄이 S1 의 문제를 "
                      "푼 자리다 — YAML 이 조용히 삼키던 것이 여기서 오류가 된다"),
                 (51, "출력은 protojson 을 yaml.Node 로 다시 그린다 — <b>필드 순서가 "
                      "알파벳순이 아니라 proto 정의 순서</b>를 따른다(사람이 읽는 순서다)"),
                 (55, "옛 매니페스트 꼴은 엄격 해독 <b>전에</b> 고쳐 넣는다 — "
                      "호환은 '느슨하게 받기' 가 아니라 '명시적으로 옮기기' 로 한다"),
             ]))

    c.날것(정리(
        "엄격 해독은 오타를 침묵에서 오류로 옮긴다",
        "매니페스트 해독기가 <b>모르는 필드를 거부</b>하면, 철자가 틀린 설정은 "
        "<b>적용 시점</b>에 실패한다. 거부하지 않으면 그 설정은 <b>기본값으로 조용히 "
        "돌고</b> 실패는 실행 중에, 다른 증상으로 나타난다.",
        [("사람이 <code>resources</code> 대신 <code>resource</code> 라고 적었다고 하자.",
          "오타는 설정 파일에서 가장 흔한 사고다"),
         ("느슨한 해독기는 모르는 키를 버린다. 남은 객체는 <code>resources</code> 가 "
          "비어 있는 멀쩡한 객체다.",
          "버린다는 것은 오류를 안 낸다는 뜻이다"),
         ("그 객체는 기본 자원 한도로 스케줄된다 — 적용은 <b>성공</b>한다.",
          "빈 칸은 기본값으로 채워지기 때문이다"),
         ("증상은 몇 분 뒤 OOM 이나 스로틀로 나타나고, 사람은 <b>자원 설정이 아니라 "
          "에이전트 코드를 의심한다.</b>",
          "증상이 원인에서 멀리 떨어진 곳에서 나타났다"),
         ("엄격 해독기는 그 키를 모른다고 <b>적용에서</b> 실패한다.",
          "protojson 의 UnmarshalOptions 가 unknown field 를 오류로 본다"),
         ("따라서 같은 오타가 '나중의 이상한 증상' 에서 '지금의 또렷한 오류' 로 옮겨간다. ∎",
          "고치는 비용은 발견이 늦을수록 커진다")],
        가정="해독기가 필드 이름의 단일 원본(proto 정의)을 가진다"))

    c.날것(사고("""<b>이 저장소도 같은 병을 앓았다.</b> `spec.py` 가 요청에서 아무것도
    못 읽었는데 <b>빈 칸을 기본값으로 채우고</b> 제안서를 냈다(100 MHz · 1.8 V).
    보고서는 정직했지만 비용은 이미 났다. 고친 방법이 AX 와 같다 &mdash;
    <b>읽히지 않으면 돌리지 않는다.</b>"""))

    # ------------------------------------------------------------------
    c.절("R2.6 조정 고리 — 한 걸음씩 '보장' 한다")

    c.날것(dia.파이프(
        [("매니페스트", "사람이 적는다", "파랑"),
         ("ax apply", "서버에 둔다", "파랑"),
         ("원하는 상태", "spec", "초록"),
         ("지금 상태", "관찰", "노랑"),
         ("차이", "원함 − 실제", "노랑"),
         ("Ensure 한 걸음", "멱등", "초록"),
         ("상태 적기", "status·조건", "회색")],
        제목="조정 고리 — 한 번에 다 맞추지 않는다",
        아래글="마지막 칸이 첫 칸으로 되돌아간다. 차이가 0 이 될 때까지 같은 바퀴를 돈다 "
             "— 한 바퀴가 실패해도 다음 바퀴가 이어받는다."))

    c.날것(dia.상태기계(
        [("Pending", 0.12, 0.5, "파랑"), ("Running", 0.38, 0.5, "초록"),
         ("Suspended", 0.64, 0.22, "노랑"), ("Failed", 0.64, 0.78, "빨강"),
         ("Terminating", 0.88, 0.5, "회색")],
        [("Pending", "Running", "작업공간 ready", 0.0),
         ("Running", "Suspended", "ax suspend", -0.25),
         ("Suspended", "Running", "ax resume", -0.25),
         ("Pending", "Failed", "Atespace 실패", 0.25),
         ("Running", "Terminating", "삭제 요청", 0.0),
         ("Failed", "Terminating", "정리", 0.2)],
        제목="Task 의 단계 — 상태는 사람이 아니라 고리가 쓴다"))

    c.날것(소스("ax_reconcile_ensure",
             "Reconcile 의 첫 두 걸음. <b>번호가 주석에 붙어 있다</b> — "
             "저자도 이것을 사다리로 보고 있다는 뜻이다.",
             풀이=[
                 (117, "<b>보장(Ensure) 이지 생성(Create) 이 아니다.</b> 이름이 멱등성을 "
                       "약속한다 — 있으면 두고, 없으면 만든다"),
                 (118, "실패하면 <code>Ready=False</code> 조건에 <b>까닭을 문자열로</b> 남긴다. "
                       "사람이 `ax get` 으로 읽을 것이 여기서 나온다"),
                 (119, "단계를 Failed 로. <b>상태는 사람이 아니라 고리가 쓴다</b>"),
                 (120, "오류를 감싸 올린다(<code>%w</code>) — Go 의 오류 사슬. "
                       "위에서 <code>errors.Is</code> 로 원인을 되짚을 수 있다"),
                 (124, "<b>actor 이름은 언제나 task 이름이다.</b> 주석이 까닭까지 적는다 — "
                       "둘을 바꿔 써도 되게 하려고. 라우터 헤더에서 그 값이 쓰인다"),
                 (126, "클라이언트가 <code>status.actor</code> 에 무엇을 적어 놨든 "
                       "<b>덮어쓴다</b> — 상태 칸은 사람의 것이 아니다"),
                 (128, "실행 아이디를 <b>없을 때만</b> 짓는다. 재조정이 다시 불려도 "
                       "같은 실행은 같은 아이디를 유지한다(멱등)"),
             ]))

    c.날것(보조정리(
        "'보장' 연산만으로 짜인 고리는 여러 번 돌아도 안전하다",
        "고리의 각 걸음이 멱등이면, 고리를 <b>중간에서 끊고 처음부터 다시 돌려도</b> "
        "최종 상태가 같다.",
        [("걸음 <i>i</i> 가 멱등이면 <i>f<sub>i</sub></i>(<i>f<sub>i</sub></i>(x)) = "
          "<i>f<sub>i</sub></i>(x) 이다.", "멱등의 정의다"),
         ("고리 한 바퀴는 합성 <i>F</i> = <i>f<sub>n</sub></i> ∘ … ∘ <i>f<sub>1</sub></i> 이다.",
          "걸음을 차례로 밟는다"),
         ("중간에서 끊겼다는 것은 어떤 <i>k</i> 까지만 밟았다는 뜻이다.",
          "프로세스가 죽는 자리는 고를 수 없다"),
         ("다시 돌리면 1..<i>k</i> 는 이미 맞은 상태에 다시 적용되고, 멱등이므로 "
          "아무것도 안 바꾼다.", "위의 정의를 그대로 쓴다"),
         ("<i>k</i>+1 부터는 처음 밟는 것과 같다.", "그 걸음은 아직 적용되지 않았다"),
         ("따라서 끊긴 실행과 안 끊긴 실행의 결과가 같다. ∎",
          "이것이 컨트롤러가 <b>재시작을 살아 넘기는</b> 까닭이다")],
        가정="걸음이 서로의 결과를 되돌리지 않는다(순서가 있어도 상쇄는 없다)"))

    c.날것(소스("ax_timeouts",
             "고리가 <b>매달리지 않게</b> 하는 상수들. 주석이 까닭을 적는다.",
             풀이=[
                 (41, "시크릿 조회에 2초 상한. 주석: <b>느리거나 닿지 않는 클러스터가 "
                      "조정을 멈춰 세우지 못하게</b>"),
                 (44, "태스크별 템플릿 이름에 스펙 다이제스트 <b>4바이트</b>만 쓴다 — "
                      "이름 길이와 충돌 확률의 맞바꿈"),
                 (48, "작업공간 준비를 15초까지 기다린다. 넘으면 '아직 초기화 중' 으로 "
                      "<b>기록하고 넘어간다</b> — 기다리다 죽지 않는다"),
                 (49, "0.5초마다 다시 본다. 폴링 간격이 상수로 박혀 있다"),
             ]))

    c.날것(짚기("""<b>모든 기다림에 상한이 있다.</b> 이 책 R1 에서 본 것과 같은 규율이다
    &mdash; 끝이 없는 기다림은 고장을 침묵으로 바꾼다. AX 는 시간 초과를 "실패" 가 아니라
    <b>"아직" 이라는 조건</b>으로 적고 다음 바퀴에 다시 본다. 그것이 조정 고리가
    예외 처리와 다른 점이다."""))

    # ------------------------------------------------------------------
    c.절("R2.7 샌드박스 안 — 러너가 하는 일")

    c.날것(소스("ax_runner_ready",
             "샌드박스 안에서 도는 쪽. <b>여기가 자료 평면이다.</b>",
             풀이=[
                 (140, "메타데이터 서버 주소를 <b>환경변수로</b> 심는다 — 안에서 도는 "
                       "에이전트가 자기 명세를 물어볼 창구다"),
                 (142, "태스크 스펙의 env 를 프로세스 환경으로 옮긴다. "
                       "<b>선언이 실행 환경이 되는 자리</b>"),
                 (146, "메타데이터 서버를 먼저 띄운다 — 작업공간 준비보다 <b>먼저</b>다. "
                       "준비 중에도 상태를 물어볼 수 있어야 하기 때문이다"),
                 (150, "<code>defer</code> 로 종료를 예약한다. Go 의 관용 — "
                       "어느 경로로 빠져나가도 서버는 닫힌다"),
                 (155, "작업공간마다 초기 설정(git clone · MCP · 스킬)을 돌린다"),
                 (157, "실패해도 <b>죽지 않는다.</b> 로그를 남기고 ready 를 내린다 — "
                       "여러 작업공간 중 하나가 실패한 것이 전부의 실패는 아니다"),
                 (161, "전부 성공했을 때만 ready 를 올린다. <b>부분 성공을 성공이라 "
                       "부르지 않는다</b>"),
             ]))

    c.날것(정리(
        "미리 데운 작업공간은 실행당 고정비를 한 번으로 줄인다",
        "작업공간 준비 비용을 <i>S</i>, 한 에이전트 실행의 일 비용을 <i>W</i> 라 하자. "
        "실행마다 준비하면 <i>n</i>회의 비용은 <i>n</i>(<i>S</i>+<i>W</i>) 이고, "
        "작업공간을 객체로 빼서 다시 쓰면 <i>S</i> + <i>nW</i> 다.",
        [("실행마다 git clone 과 MCP 연결을 다시 한다면 매 실행이 <i>S</i> 를 문다.",
          "찬 시작의 정의다"),
         ("Workspace 를 Task 와 <b>따로</b> 선언하면 여러 Task 가 같은 이름을 가리킨다.",
          "매니페스트에서 참조이지 복사가 아니다"),
         ("그러면 준비는 작업공간 수명당 한 번이고 실행은 그 위에 얹힌다.",
          "러너가 mount 로 받아 쓰기 때문이다"),
         ("절약은 (<i>n</i>−1)<i>S</i> 이고, <i>S</i>/<i>W</i> 가 클수록 이익이 크다. ∎",
          "짧은 일을 많이 돌릴수록 준비 비용의 비중이 커진다")],
        가정="작업공간의 상태가 실행들 사이에서 오염되지 않는다 — "
           "이것이 성립하지 않으면 격리를 포기한 것이다"))

    c.날것(사고("""<b>그 가정이 이 설계의 약한 고리다.</b> 작업공간을 공유하면 빨라지고,
    공유하면 한 에이전트가 남긴 찌꺼기를 다음 에이전트가 본다. AX 는 이것을 코드로 풀지
    않고 <b>사람이 고르게</b> 둔다 &mdash; Workspace 를 여럿이 쓸지 하나만 쓸지는
    매니페스트에 적는 값이다. 교과서가 답을 주지 않는 자리이고, 그래서 여기 적는다."""))

    # ------------------------------------------------------------------
    c.절("R2.8 이 책의 다른 장과 어디서 만나나")

    c.날것(표("AX 가 새로 낸 것과, 이 책이 이미 세운 것",
           ["AX 의 것", "이 책의 어디", "관계"],
           [["조정 고리 · 원하는 상태", "R1 상태그래프",
             "둘 다 <b>상태를 밖으로 꺼낸다.</b> R1 은 한 실행 안, AX 는 클러스터 전체"],
            ["suspend / resume", "R1 체크포인트·재개",
             "같은 정리가 적용된다 — <b>중단과 재개는 안 끊긴 실행과 같아야 한다</b>"],
            ["엄격 매니페스트 해독", "S1 선언형·YAML 암묵변환",
             "S1 이 낸 <b>문제</b>를 AX 가 <b>푼</b> 자리. proto 를 원본으로 삼는다"],
            ["Gateway 송출 허용목록", "W6 거부목록은 과속방지턱이다",
             "<b>허용목록은 경계이고 거부목록은 아니다.</b> AX 가 허용목록을 고른 것이 "
             "그 정리와 맞는다"],
            ["Task 의 자원 한도", "M1 능력 천장",
             "천장을 올리는 장치가 아니라 <b>바닥을 지키는</b> 장치다 — 다른 축이다"],
            ["Model 이 시크릿을 가리킴", "W1 출력 계약",
             "선언에 담을 것과 안 담을 것의 경계. 둘 다 <b>어디서 강제하는가</b>의 문제"]]))

    c.날것(사고("""<b>AX 를 읽고 우리가 가져올 것 하나.</b> 이 저장소의
    `house/discord_cmd.py` 는 요청을 받으면 바로 돌렸다. AX 라면 그것은
    <b>적용(apply) 시점 검증이 없는 것</b>이다. 2026-09-21 에 31자짜리 요청으로 빈
    제안서가 나온 사고가 정확히 그 꼴이었고, 고친 모양은 AX 의 엄격 해독과 같다 &mdash;
    <b>읽히지 않는 선언은 받지 않는다.</b>"""))

    c.날것(짚기("""<b>알파다.</b> README 가 "핵심 개념·프로토콜·명세를 아직 다듬는 중이고
    안정판 전에 깨지는 변경을 낼 것" 이라고 적었다. 이 장의 줄 번호와 필드 이름은
    <b>커밋 d8ed0fe 의 것</b>이다. 쓰기 전에 그 커밋과 지금을 견주어라."""))

    return c.완성()
