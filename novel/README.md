# `novel/` — 검증된 장편 소설 생성 오케스트레이션

이 저장소의 다른 오케스트레이션(`orchestrator/`, `mathgen/`, `compression/`)과 **같은 원칙**
위에 서 있다: **생성자와 심판을 분리하고, 심판은 절대 LLM 이 아니다.**

소설에서 그것이 왜 가능한가. "재미있는가" 는 기계가 못 본다. 그러나 **"세계가 자기모순인가"
는 정확히 볼 수 있다** — A 와 B 가 사귀는데 어느 순간 C 가 A 와 사귄다, 죽은 인물이
다음 화에 걸어 들어온다, 모르는 비밀을 인물이 말한다. 이것들은 취향이 아니라 사실이다.
그래서 여기서는 취향을 판정하지 않고 **모순만** 잡는다.

## 30초 요약

```bash
python3 novel/seed.py --n 10                       # 씨앗 열 개 뽑아 읽는다 (LLM 안 씀)
python3 novel/world_seeded.py --new                # 하나 뽑아 세계로 편다 (LLM 안 씀)
python3 novel/overnight.py --world seeded --hours 2  # 실제 집필
python3 novel/watch.py --path novel/seeded.json -f   # 진행 보기
python3 novel/read.py  --path novel/seeded.json --ep 1-3   # 읽기
python3 novel/profile.py --path novel/seeded.json    # 병목 재기
```

## 파이프라인

```
씨앗(코드)      축 7개를 무작위 조합 -> 9,400만 가지. 기계가 최소한만 거른다
   ↓
세계(코드)      인물·비밀·법·목소리·결말 블록으로 편다
   ↓
조립            결말 하나를 회차들로 **역방향**으로 편다 (원인은 결과 뒤에 찾는다)
   ↓ 디렉터(Opus, claude -p) : XML 입력 -> Markdown 시나리오
   ↓ 추출기(Gemini)          : Markdown -> JSON
집필            씬마다 배우 턴 -> 관문 -> 화자 산문 -> 관문 -> 수리
   ↓ 배우·화자(Gemini)
관문 10개       모순만 본다. 위반 목록을 돌려준다. hard 는 기각, soft 는 기록만
```

**형식 세금**: 창작은 Markdown 으로 받고 구조화는 별도 호출로 뽑는다. 출력 형식을 JSON 으로
강제하면 모델이 문장을 지으면서 동시에 문법을 지켜야 해서 개방형 창작의 품질이 떨어진다.
JSON 의 우위는 **추출 태스크에 한정**된 이야기라, 두 태스크를 갈라 각자 유리한 형식을 쓴다.

## 파일 지도

| 파일 | 하는 일 |
|---|---|
| `seed.py` | 자유 씨앗. 축 7개(시간·불가능·인물·사건·장치·목소리·주제) 조합 + 최소 검사 + 조사 처리기 |
| `world_seeded.py` | 씨앗 -> `Novel` + 결말 블록. **LLM 안 씀** |
| `world_romance.py` | 손으로 쓴 200화 세계(음대 로맨스). 15블록 + steps 45개 |
| `world_probe.py` | 3화짜리 시험대. 디렉터 8회로 배선을 싸게 확인한다 |
| `state.py` | `Novel`/`Scene`/`Character`. 관계·지식·설정 원장, 동적 게이트 도출, **시간선(`timeline`)** |
| `verbs.py` | 세계를 바꾸는 동사 53개의 **닫힌 집합**. 여기 없는 동사는 hard 위반 |
| `gate.py` | 관문 10개. 전부 기계 검사이고, **모순만** 본다 |
| `arc.py` | 3막 8시퀀스, 회차 5,000자, 클리프행어 5공식 |
| `episode.py` | 역방향 조립. `Beat`/`Outcome`/`Episode` |
| `drive.py` | 씬 루프와 프롬프트 전부. 디렉터/배우/화자/추출기 |
| `overnight.py` | 무인 러너. 예산·건너뛰기·동시 실행 차단 |
| `read.py` `watch.py` `profile.py` `repair_ops.py` | 읽기 · 진행 보기 · 병목 측정 · 저장된 원고 수리 |

검사: `tests/test_novel_*.py`, `test_agency.py`, `test_timeline.py`, `test_seed.py`,
`test_probe_world.py`, `test_json_resilience.py` — CI(`.github/workflows/gates.yml`)에 물려 있다.

## 관문 10개 — 모순만 본다

2026-09-04 축소. 취향에 속하는 검사 13개를 뺐다. 남은 것은 전부 **세계가 자기모순인가**를
묻는다 — 사실이지 의견이 아니므로 기계가 판정할 자격이 있는 것들이다.

| | 무엇 | | 무엇 |
|---|---|---|---|
| V001 | 턴 형식 · 등장인물에 없는 화자 | V010 | **설정 모순** — 같은 키의 다른 값 |
| V004 | 시점 위반(1인칭이 타인 내면을 단정) | V011 | 믿음과 사실의 분리 |
| V007 | 화자 없는 씬을 1인칭이 서술 | V012 | 공개된 허구(위장·폭로의 구조) |
| V008 | **지식 누출** — 모르는 걸 말하거나 생각한다 | V018 | **개연성 사슬**(그래프 도달성) |
| V009 | **관계 모순** — 배타성·시작 없는 종료 | V023 | 시계 되감김(같은 마감 안에서) |

**뺀 것**: V002 감정 급변 · V003 감정 폭/봉투 · V005 직접 감정 서술 · V006 푼크툼 ·
V013 시퀀스 궤도 · V014 꺾임 · V015 사건 규모 · V016 정보 격차 · V017 클리프행어 ·
V019 회차 분량 · V020 문장 리듬 · V022 능동성 · V025 회차가 여는가.

왜 뺐나. 이것들은 "무엇이 좋은 소설인가" 에 대한 관문의 의견이었지 모순이 아니다. 그리고
실측에서 되돌려보내기 1·2위(V003 25회, V009 16회)가 산문 수리로는 못 고치는 선언 문제였고
집필 시간의 100% 가 수리에 갔다. 관문이 작가가 되면 원고는 관문을 통과하는 방향으로
균질해진다 — 아래 6번이 이미 적어둔 것이다.

**규율이 사라진 것은 아니다.** 분량·리듬·푼크툼·능동성은 화자/디렉터 프롬프트와 조립 단계의
강제(`drive._check_pressure`, 비트의 `driver`/`cost` 요구)에 그대로 남아 있다. 없앤 것은
**기각 권한**이다.

## 이 설계가 배운 것 (2026-09-04 실측)

전부 실제로 밤을 날리거나 원고를 지운 것들이다. 다음 사람이 같은 곳에 빠지지 않게 적는다.

1. **산문 수리로 못 고치는 것을 산문 수리 루프에 넘기지 마라.**
   V003 이 감정 봉투를 **씬마다** 물어 25회, V009 가 선언(`relation_ops`)의 문제로 16회
   되돌려보냈다. 문장을 백 번 고쳐도 선언은 안 바뀌므로 시도 횟수를 다 쓰고 결정론적으로
   실패한다. 선언은 **조립 시점**에 되먹임으로 강제하고, 씬 관문에서는 soft 로만 본다.

2. **좌표를 하나 더 주면 모순이 사라진다.**
   `misbelieve` 가 진실과 믿음을 갈라 "X 를 믿는데 사실은 Y" 를 모순이 아니게 만들었다.
   시간 되감기도 같다 — `Scene.branch` 와 `Novel.timeline()` 이 시간선을 가르고, 되감은
   사람만 기억을 갖고 넘어온다(`carry`). 그 비대칭이 정보 격차를 통째로 만든다.

3. **requires 와 steps 는 다르다.**
   하나로 겸했더니 앞 블록이 갚아버려 열린 조건이 0이 되고, 척추가 결말 하나로 끝났다 --
   200화 중 585씬이 인과 없는 곁가지였다. `requires` 는 블록 간 의존성, `steps` 는 이 블록
   안에서 세울 단계다.

4. **같은 프롬프트는 같은 답을 낸다.**
   서브플롯 프롬프트에 "이미 쓴 것" 을 안 실었더니 세 회차가 전부 같은 소재였다. 겹치지
   말라는 지시는 **비교 대상이 있어야** 지킬 수 있고, 그것마저 절반쯤 지켜지므로
   difflib 로 재서 되돌려보낸다.

5. **분량 할당량은 희석 지시다.**
   화자 프롬프트의 "짧게 끊지 마라 -- 정적·냄새·소리로 채워라" 가 곧 "채워라" 다.
   프로토타입(2,000자)이 더 재미있었던 이유의 절반이 여기 있다.

6. **관문은 바닥을 지키지 천장을 만들지 않는다.**
   다 통과한 텍스트가 지루할 수 있다. 재미는 씨앗(무엇을 쓸 것인가)과 디렉터
   시나리오에서 나오고, 관문은 그것이 무너지지 않게만 한다.

7. **운영에서 겪은 것**: 두 벌이 같은 원고를 쓰면 나중에 저장한 쪽이 상대의 산문을 통째로
   지운다(코드가 막는다). `pgrep -af` 는 `test_overnight.py` 도 잡는다. 알림 실패가 런을
   죽이면 안 된다. `claude -p` 는 부모를 죽여도 타임아웃까지 남는다.

## 지금 상태와 다음 할 일

**됨**: 씨앗 -> 세계 -> 조립 -> 집필 -> 관문 -> 읽기까지 한 바퀴가 실제로 돈다.
탐침 3화 기준 산문이 나오고 관문을 통과한다.

**안 됨 / 다음**:
- 척추 비율이 13% 다(목표 33%). 블록당 steps 를 3 -> 9로 늘리면 닿는다
- 자유 집필 모드 — 배우 JSON 을 거치지 않고 화자가 회차를 통째로 쓴 뒤 추출하는 경로.
  위 5번·6번의 근본 대응이다
- 3층 관문(스태터스·설명덤프·리듬 강화)은 설계만 있고 미구현
- 속도: 497자/분 실측 -> 100만자에 34시간. 단계별 수리를 넣어 줄이는 중

## 주의

- 원고(`novel/*.json`)와 씨앗 원장은 `.gitignore` 에 있다. 서버가 계속 쓰는 파일이라
  추적하면 배포의 `git pull` 이 매번 충돌한다
- 무인 런은 반드시 `setsid nohup ... & disown` 으로 띄운다(`CLAUDE.md` 참고).
  `ps -p $!` 로 확인하면 거짓 음성이 난다 — `pgrep -af <스크립트명>` 을 쓴다
