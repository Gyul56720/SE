"""말맛 장부 -- **쓴 것은 세어 두고, 안 쓴 쪽으로 민다.**

문제. 화자는 편하게 쓸 수 있는 것부터 쓴다. 종결어미는 '-다/-었다' 로 몰리고, 비유는
직유 하나로 몰리고, 문장 성분은 '주어 + 목적어 + 서술어' 로 몰린다. 한 덩어리만 보면
안 보이는데 스무 덩어리를 이어 읽으면 같은 소리가 난다. 그것이 "글이 비슷하다" 의 정체다.

고치는 법. **하한을 더 두지 않는다** -- 하한을 두면 하한을 정확히 맞춘다는 것을 이미
겪었다(짧은 '-다' 62%, 긴 대사 여덟 할). 대신 **원고에서 실제로 쓰인 횟수를 세어**,
적게 쓴 것을 이번 덩어리에 밀어 넣는다. 많이 쓴 것은 이번엔 빼라고 한다.

이건 금지가 아니라 **복원력**이다. 한쪽으로 기울면 반대쪽으로 미는 힘이고, 기울지
않았으면 아무 일도 하지 않는다. 목록에 없는 것을 쓰는 것은 언제나 자유다.
"""
from __future__ import annotations

import os
import re

# 종결 어미. **뜻이 아니라 소리로 가른다** -- 같은 뜻이라도 말끝이 다르면 다른 문장이다.
ENDINGS = {
    # **겹쳐 세지 않는다.** '-겠다 / -것이다 / -었다' 까지 '-다' 로 세면 '-다' 는 언제나
    # 과반이 되어 매번 "쉬어라" 가 나온다 -- 늘 켜져 있는 경고는 꺼져 있는 것과 같다.
    "-다":      re.compile(r"(?<![었았였겠는ㄴ이])다[.!?…]"),
    "-었다":    re.compile(r"(었|았|였)다[.!?…]"),
    "-ㄴ다":    re.compile(r"[가-힣](ㄴ다|는다)[.!?…]"),
    "-겠다":    re.compile(r"겠다[.!?…]"),
    "-것이다":  re.compile(r"(것|거)(이|)다[.!?…]"),
    "-까":      re.compile(r"[가-힣](까|을까|ㄹ까)[.?…]"),
    "-지":      re.compile(r"[가-힣]지[.?…]"),
    "-군":      re.compile(r"[가-힣](군|구나|네)[.!…]"),
    "-야":      re.compile(r"[가-힣](야|이야|아)[.!…]"),
    "명사로":   re.compile(r"[가-힣](음|기|것|뿐|만|채|척|둥)[.…]"),
    "말줄임":   re.compile(r"…"),
    "-라":      re.compile(r"[가-힣](라|아라|어라)[.!…]"),
}

# 비유. 이름이 아니라 **꼴**로 가른다 -- 은유를 '은유' 라고 부르면 진단서가 된다.
FIGURES = {
    "직유":   "'-처럼 / -같이 / -듯이' 로 견준다",
    "은유":   "견주는 말 없이 그냥 그것이라고 한다",
    "환유":   "옆에 있는 것으로 대신 부른다 -- 사람을 그 사람의 신발로 부르듯",
    "의인":   "사람 아닌 것이 사람처럼 군다",
    "과장":   "터무니없이 키운다. 본인은 진지하다",
    "축소":   "큰일을 사소한 것처럼 말한다",
    "감각바꿈": "소리를 색으로, 냄새를 무게로 -- 감각을 건너뛴다",
    "숫자로":  "재서 말한다 -- 몇 시 몇 분, 몇 미터, 몇 번째",
}

# **글의 꼴 자체를 흔든다.** 문장만 흔들면 원고는 여전히 같은 모양이다 -- 다 산문이고,
# 다 3인칭이고, 다 한국어 어순이다. 여기 있는 것들은 규칙이 아니라 **재료**다. 이번
# 덩어리에 하나쯤 섞으라는 것이지, 매번 다 하라는 뜻이 아니다.
FORMS = {
    "번역체":   "영어로 먼저 생각하고 옮긴 듯한 문장 -- 관계대명사가 남고, 수동태가 남고,"
                " '그것은 ~한 것이었다' 가 남는다",
    "목록":     "번호나 줄표로 늘어놓는다. 소설 한가운데 목록이 끼면 그 자체가 사건이다",
    "문서":     "영수증 · 시간표 · 규정 · 안내문 · 진단서를 그대로 옮겨 적는다",
    "숫자판":   "재서 말한다. 몇 시 몇 분, 몇 미터, 몇 호, 몇 번째 -- 정확할수록 진짜 같다",
    "기호":     "괄호, 줄표, 별표, 화살표를 문장 안에 그대로 쓴다",
    "말없음":   "한 줄이 통째로 공백이거나 점 세 개다. 아무 설명도 없다",
    "이인칭":   "'너' 라고 부른다. 누가 누구에게 하는 말인지는 밝히지 않는다",
    "현재형":   "지금 일어나는 일처럼 쓴다 -- 시제를 한 대목만 바꾼다",
    "인용":     "누가 한 말을 옮겨 적고, 그 말이 맞는지는 따지지 않는다",
    "각주":     "본문 옆에 딴 얘기를 단다. 딴 얘기가 본문보다 중요하다",
    "되감기":   "끝을 먼저 말하고 어쩌다 그리 됐는지로 간다",
    "겹말":     "같은 문장을 조금씩 바꿔 가며 세 번 쓴다. 셋째에서 뜻이 달라진다",
}
PUSH_FORM = int(os.environ.get("DRIFT_PUSH_FORM", "1"))

# **감정에 이름을 붙이는 말.** 화자가 이걸 쓰면 그 순간 진단서가 된다 -- 슬펐다고 적는
# 대신 무엇을 했는지 적으면 읽는 사람이 알아서 안다. 대사 안에서는 자유다(사람은 자기
# 기분을 말하니까). 서술문에서만 센다.
FEELING = re.compile(
    r"(기뻤|슬펐|슬프|화가 나|화났|짜증|불안|초조|외로|그리웠|그립|서운|억울|"
    r"두려|무서웠|무섭|설레|긴장|당황|허탈|참담|비참|절망|행복|후회|미안했|"
    r"부끄러|창피|안도|안타까|answer없)")
# **상한을 두지 않는다.** 몇 개까지라고 못 박으면 그 숫자가 다시 규칙이 되고, 무엇보다
# 감정을 말하는 것 자체는 잘못이 아니다 -- 대사로는 얼마든지 해도 좋다. 여기서 보는 것은
# **기울기**다: 서술문이 최근 들어 감정에 이름을 자주 붙였으면 그만큼 되민다.

# **세계를 넓히는 재료.** 한 세계관 안에만 있으면 원고가 한 가지 색이 된다. 제도와 매체는
# 사람을 움직이게 하는 실제 장치이고, **제한**은 편의주의를 막는 장치다 -- 우연이 문제를
# 풀지 못하게 하려면 풀 수 없는 이유가 구체적이어야 한다.
SYSTEMS = (
    "편입 시험 -- 원서 마감, 반영 비율, 미달 학과, 재수강 학점",
    "상속과 등기 -- 서류 한 장이 없어서 몇 달이 간다",
    "병원 수속 -- 보호자 동의, 전원, 병상 대기, 진단서 발급 수수료",
    "보험 -- 약관에 안 적힌 것, 면책 기간, 손해사정인",
    "장례 절차 -- 발인 시각, 부의금 장부, 상주 순서",
    "군대·병역 -- 신체 등급, 소집 해제, 예비군 훈련 통지",
    "세입 -- 보증금, 확정일자, 계약 갱신 거절 통보",
    "면허와 자격 -- 갱신 기간, 실기 재응시, 결격 사유",
    "채무 -- 이자율, 독촉장, 연대보증",
    "관공서 -- 민원 접수 번호, 담당자 부재, 반려 사유",
    "회사 -- 수습 기간, 인사 발령, 시말서, 퇴직금 정산",
    "학교 -- 출석 일수, 전학 절차, 생활기록부",
)
MEDIA = (
    "편지 -- 부치지 않은 것이든, 늦게 온 것이든, 남의 것이든",
    "영화 -- 본 것을 잘못 기억하고 옮긴다",
    "드라마 -- 다음 회를 기다리는 사람이 있다",
    "책 -- 밑줄이 그어져 있고, 그은 사람은 여기 없다",
    "녹취 -- 목소리만 남고 얼굴이 없다",
    "사진 -- 찍힌 사람과 찍은 사람 중 하나가 빠져 있다",
    "신문 기사 -- 짧게 실렸고 틀린 데가 있다",
    "문자와 통화 기록 -- 안 받은 것이 남는다",
    "일기 -- 날짜가 띄엄띄엄하다",
    "간판과 안내문 -- 오래돼서 지금과 안 맞는다",
)
LIMITS_LIFE = (
    "돈이 모자란다. 얼마가 모자란지 정확히 안다",
    "시간이 정해져 있다 -- 몇 시까지, 며칠까지",
    "규정이 막는다. 규정을 만든 사람은 여기 없다",
    "거리가 멀다. 가는 데 얼마가 걸리는지 안다",
    "몸이 안 따라 준다 -- 다쳤거나, 늙었거나, 못 잔다",
    "누가 허락해야 한다. 그 사람이 연락이 안 된다",
    "서류가 하나 없다. 그거 하나 때문에 전부 멈춘다",
    "말이 안 통한다 -- 말씨든, 전문 용어든, 나이 차든",
)
PUSH_WORLD = int(os.environ.get("DRIFT_PUSH_WORLD", "1"))

# **외현 -- 밖에서 오는 것.** 내현(속으로 느끼고 생각하는 것)만 쌓이면 원고가 한 사람의
# 머릿속이 되고, 그러면 무슨 일이 어디서 벌어지는지 안 보인다. 밖은 **이름과 숫자와
# 남의 행동**으로 온다. 여기 있는 것은 재료지 목록이 아니다 -- 하나만 골라도 된다.
OUTSIDE = (
    "**간판과 상호** -- 글자가 몇 개 나갔고, 옆 가게는 뭘 파는지, 언제 바뀌었는지",
    "**건물** -- 몇 층인지, 무슨 자재인지, 입구가 어느 쪽인지, 누구 이름이 붙어 있는지",
    "**인쇄물** -- 잡지·전단·시간표·명함. 거기 적힌 이름과 날짜를 그대로 읽는다",
    "**제3자의 행동** -- 상관없는 사람이 지나가며 뭔가를 한다. 설명은 없다",
    "**소리** -- 어디서 나는지, 무엇이 내는지. 사람 말이 아닌 소리",
    "**날씨와 시각** -- 몇 시인지 알 수 있게. 빛이 어느 쪽에서 오는지",
    "**탈것** -- 번호, 노선, 몇 번째 칸, 얼마나 밀리는지",
    "**값** -- 얼마인지. 값이 붙은 것은 그 자체로 세계다",
    "**남의 대화** -- 지나가다 들린 반 토막. 무슨 얘긴지 모르는 채로 지나간다",
    "**몸에 닿는 것** -- 앉은 자리, 든 것의 무게, 손에 묻은 것",
    "**표지와 안내** -- 화살표, 금지 표시, 층별 안내. 누가 언제 붙였는지",
    "**남이 걸친 것** -- 옷, 신발, 명찰. 거기서 그 사람의 사정이 읽힌다",
)
PUSH_OUT = int(os.environ.get("DRIFT_PUSH_OUT", "3"))

# **바깥 말에서 끌어오는 것.** 한국어 안에만 있으면 문장이 갈 수 있는 데가 한정된다.
# 영어(를 비롯한 남의 말)에는 한국어에 없는 **틀**이 있고, 그것을 그대로 들여오면
# 어색한데 그 어색함이 문체가 된다 -- 번역체가 한 시대의 문체였던 것처럼.
# 여기 적은 것은 **범주**지 예문이 아니다. 무엇을 쓸지는 그때 정한다.
FOREIGN = (
    "**관계절을 뒤에 매단다** -- 한국어는 꾸미는 말이 앞에 오는데, 영어처럼 뒤에서"
    " 이어 붙여 본다. 문장이 길어지고 숨이 달라진다",
    "**도치** -- 말할 것을 먼저 던지고 주어를 뒤에 둔다. 강조가 어순으로 온다",
    "**한국어에 없는 낱말을 그대로 쓴다** -- 옮기면 뜻이 새는 말이 있다."
    " 옮기지 말고 쓰고, 옮기지 않은 이유를 인물이 말하게 해라",
    "**직역한 관용구** -- 남의 말 속담이나 숙어를 곧이곧대로 옮긴다. 뜻이 반쯤 어긋난 채로",
    "**약어와 라틴어 꼬리** -- 문서에 붙는 짧은 표기들. 인물이 그 뜻을 잘못 안다",
    "**남의 말 문화의 절차** -- 회신 요망, 팁, 예약 확인, 서명란, 날씨 인사."
    " 이쪽에서는 안 하는 짓이라 어긋난다",
    "**단위가 다르다** -- 저쪽 단위로 적힌 것을 이쪽 사람이 어림으로 옮긴다. 틀린다",
    "**이름 표기** -- 성과 이름의 순서, 가운데 이름, 애칭. 부르는 쪽이 헷갈린다",
    "**존댓말이 없는 말에서 옮겨 온 대사** -- 누구에게 하는 말인지가 흐려진다",
    "**두 말을 섞어 쓰는 사람** -- 한 문장 안에서 갈아탄다. 본인은 자연스럽다고 여긴다",
    "**자막과 더빙** -- 본 것을 옮겨 말하는데 원래 대사와 다르다",
    "**남의 말 문법을 잘못 아는 사람** -- 틀린 규칙을 진지하게 가르친다",
)
PUSH_FOREIGN = int(os.environ.get("DRIFT_PUSH_FOREIGN", "1"))

# **어디서 온 개념이든 끌어온다.** 위가 남의 말에서 끌어오는 것이라면, 이쪽은 남의
# **분야**에서 끌어오는 것이다. 소설은 무엇이든 삼킬 수 있다 -- 통계 용어든, 뱃사람
# 은어든, 옛날 도량형이든. 인물이 그것을 **자기 삶에 갖다 붙이는 순간** 그 낱말은
# 설정이 아니라 성격이 된다.
#
# 여기 적은 것은 **어디를 뒤질지**지 무엇을 쓸지가 아니다. 구체적인 것은 그때 정한다.
ODD = (
    "**전문 분야의 용어** -- 법·의학·공학·회계·항해·건축 중 하나. 인물이 그것으로 일상을 잰다",
    "**셈과 확률의 말** -- 평균 · 표본 · 오차 · 기댓값. 사람 일에 갖다 붙이면 어긋난다",
    "**옛 도량형과 옛말** -- 지금 안 쓰는 단위와 낱말. 나이 든 사람만 그대로 쓴다",
    "**직업 은어** -- 그 바닥에서만 통하는 말. 밖에서는 뜻이 안 통해서 되묻게 된다",
    "**놀이와 내기의 규칙** -- 판돈, 순서, 벌칙. 사람 관계가 그 규칙을 닮아 간다",
    "**미신과 금기** -- 하면 안 되는 것들. 왜 안 되는지는 아무도 모른다",
    "**의례의 순서** -- 제사 · 예배 · 개업 · 졸업. 순서가 틀리면 사람들이 불편해한다",
    "**방언과 억양** -- 어디 사람인지가 말에서 드러난다. 본인은 못 느낀다",
    "**망 속의 말** -- 줄임말 · 밈 · 유행어. 나이에 따라 아는 사람과 모르는 사람이 갈린다",
    "**분류와 등급** -- 몇 급, 몇 종, 몇 등급. 사람을 그렇게 부르면 그것이 사건이다",
    "**기계와 절차의 말** -- 대기열 · 우선순위 · 재시도 · 복구. 사람 일을 그렇게 설명한다",
    "**철학과 논리의 말** -- 전제 · 반례 · 필요조건. 싸움에 갖다 쓰면 이상해진다",
    "**지도와 방위** -- 좌표 · 등고선 · 축척. 아는 사람에게는 세계가 그렇게 보인다",
    "**악보와 소리의 말** -- 박자 · 음정 · 쉼표. 말과 걸음에 갖다 붙인다",
    "**농사와 계절의 말** -- 절기 · 파종 · 수확. 도시 사람이 그 말을 잘못 쓴다",
    "**옛 기록의 말투** -- 등본 · 족보 · 판결문 · 비문. 그 문장에는 감정이 없다",
)
PUSH_ODD = int(os.environ.get("DRIFT_PUSH_ODD", "2"))

# 몇 개를 밀어 넣을 것인가. 너무 많이 시키면 안 지켜진다(한꺼번에 시키면 안 지켜진다).
PUSH_END = int(os.environ.get("DRIFT_PUSH_END", "3"))
PUSH_FIG = int(os.environ.get("DRIFT_PUSH_FIG", "2"))
# 전체의 이만큼을 넘게 차지하면 "이번엔 쉬어라" 고 한다.
HOG = float(os.environ.get("DRIFT_HOG", "0.35"))


def count(text: str) -> dict:
    """어떤 말끝을 몇 번 썼는가. 겹쳐 세도 된다 -- 몫이 아니라 **기울기**를 보는 것이다."""
    return {k: len(p.findall(text)) for k, p in ENDINGS.items()}


def tally(chunks) -> dict:
    """원고 전체의 말끝 셈."""
    out = {k: 0 for k in ENDINGS}
    for c in chunks:
        for k, v in count(c).items():
            out[k] += v
    return out


def thin(chunks, k: int = PUSH_END) -> list:
    """**적게 쓴 말끝** k 개. 원고가 없으면 아무것도 안 민다 -- 밀 방향이 없다."""
    if not chunks:
        return []
    t = tally(chunks)
    return [x for x, _ in sorted(t.items(), key=lambda kv: (kv[1], kv[0]))[:k]]


def hogs(chunks) -> list:
    """**너무 많이 쓴 말끝.** 전체의 HOG 를 넘게 차지한 것들."""
    if not chunks:
        return []
    t = tally(chunks)
    total = sum(t.values())
    if total < 20:                     # 표본이 적으면 기울기를 말할 수 없다
        return []
    return [k for k, v in sorted(t.items(), key=lambda kv: -kv[1]) if v / total > HOG]


def figures(seed: str, n: int, k: int = PUSH_FIG) -> list:
    """이번 덩어리에 쓸 비유의 꼴. **뽑아서 흔든다** -- 세는 것은 말끝만 한다.
    비유는 정규식으로 셀 수 없어서(은유를 어떻게 찾겠는가) 대신 매번 다르게 뽑는다."""
    from novel import shock
    names = tuple(FIGURES)
    return list(shock._batch(names, f"{seed}|figure", n, "fig", k))


def forms(seed: str, n: int, k: int = PUSH_FORM) -> list:
    """이번 덩어리에 섞을 글의 꼴. 비유와 같은 이유로 세지 않고 뽑는다."""
    from novel import shock
    return list(shock._batch(tuple(FORMS), f"{seed}|form", n, "form", k))


def feel_rate(text: str) -> float:
    """**서술문에서** 감정에 이름을 붙인 비율. 대사는 안 센다 -- 사람은 자기 기분을
    말하고, 그건 대사가 할 일이다. 화자가 대신 말해 버리면 그때 진단서가 된다."""
    from novel import rhythm
    tell, _ = rhythm._lines(text)
    if not tell:
        return 0.0
    return sum(bool(FEELING.search(s)) for s in tell) / len(tell)


def feel_push(chunks) -> str:
    """**프롬프트에는 안 싣는다.** 재 두기만 한다.

    한때 이것을 프롬프트로 밀었는데, 그것은 초고의 감정을 단속하는 짓이었다 -- 초고에는
    감정이 있어도 좋다. 여기서 잡을 것은 내용이 아니라 방향과 꼴이다. 숫자는 남겨 둔다:
    원고가 어느 쪽으로 기울었는지 나중에 볼 때 쓴다.

    기울었으면 되민다. 안 기울었으면 아무 말도 안 한다.

    무엇과 견주는가 -- **이 원고 자신**이다. 바깥에서 가져온 숫자를 기준으로 삼으면
    그 숫자가 다시 하드코딩이다. 최근 몇 덩어리가 원고 전체 평균보다 도드라지면
    그때만 민다. 원고가 처음부터 담담했으면 여기서 할 일이 없다."""
    if len(chunks) < 3:
        return ""
    rates = [feel_rate(c) for c in chunks]
    whole = sum(rates) / len(rates)
    recent = sum(rates[-3:]) / 3
    if recent <= whole or recent < 0.05:
        return ""
    return ("  · **이번엔 화자가 감정에 이름을 붙이지 마라** -- 최근 서술문에서 "
            f"{recent:.0%}가 그렇게 끝난다(원고 평균 {whole:.0%}).\n"
            "      슬펐다고 적지 말고 **무엇을 했는지** 적어라. 읽는 사람이 알아서 안다.\n"
            "      **대사에서는 자유다** -- 사람은 자기 기분을 말한다. 화자가 대신 말하는 "
            "것만 문제다.")


def _out_share(seed: str, n: int) -> float:
    """이번 덩어리에서 **밖(외현)이 차지할 몫.** 고정하지 않는다 -- 반반으로 박아 두면
    매 덩어리가 반반이 되고, 그 자체가 한 가지 가락이다."""
    from novel import rhythm
    return rhythm.wave(f"{seed}|outshare", n, 0.25, 0.75, 2)


def brief(chunks, seed: str, n: int) -> str:
    """이번 덩어리에 밀어 넣을 것. 기울지 않았으면 짧아지고, 기울었으면 길어진다."""
    lines = []
    few = thin(chunks)
    if few:
        lines.append("  · **이번 대목에서 써 볼 말끝** -- " + " · ".join(few)
                     + "\n      원고 전체에서 적게 쓴 것들이다. 억지로 다 쓰지는 마라 --"
                       " 문장이 그리로 가면 그리로 가게 두라는 뜻이다.")
    over = hogs(chunks)
    if over:
        lines.append(f"  · **이번엔 쉬어라** -- {' · '.join(over)}"
                     "\n      원고에서 이미 너무 많다. 같은 소리가 계속 나면 그게 단조로움이다.")
    fig = figures(seed, n)
    if fig:
        body = " / ".join(f"**{f}**({FIGURES[f]})" for f in fig)
        lines.append(f"  · **이번 대목의 비유** -- {body}"
                     "\n      한 덩어리에 한둘이면 족하다. 매 문단에 넣으면 그것도 버릇이다."
                     " 비유의 재료는 **이 세계 안에 이미 있는 것**에서 가져와라.")
    lines.append(
        "  · **지어내되 있을 법하게.** 여기 있는 것들은 자세해지라는 것이지 이상해지라는"
        " 것이 아니다.\n"
        "      없는 것을 지어내도 되지만 **현실의 테두리 안**이어야 한다 -- 지명·상표·규정·"
        "값·직함이 실제로 그런 것이 있을 법한 꼴이어야 한다.\n"
        "      자세함이 그럴듯함을 만든다: 왜 그렇게 불리는지, 누가 언제 그렇게 정했는지,"
        " 얼마인지, 몇 번째인지. **재서 말하면 진짜가 된다.**\n"
        "      마법도 초능력도 없다. 이상한 일이 일어나도 **사람이 저질러서** 일어난다.")
    from novel import shock
    # **외현을 먼저 놓는다.** 뒤에 묻으면 안 지켜진다 -- 이 원고가 안으로만 파고들던
    # 이유가 밖을 시키는 자리가 없었기 때문이다.
    out_pick = shock._batch(OUTSIDE, f"{seed}|outside", n, "outside", PUSH_OUT)
    lines.append(
        "  · **밖을 보여라(외현)** -- 이번 대목에서 쓸 것:\n"
        + "\n".join(f"      - {x}" for x in out_pick)
        + f"\n      **이번 대목은 밖이 {_out_share(seed, n):.0%}, 안이 나머지다.** 이 몫은"
          " 덩어리마다 다르다 -- 어떤 대목은 거의 다 밖이고, 어떤 대목은 거의 다 안이다."
        + "\n      속으로 파고드는 것(내현)은 밖에 부딪혀야 뜻이 생긴다. **이름과 숫자로**"
          " 놓아라 -- '낡은 건물' 이 아니라 몇 층에 뭐라고 적힌 건물인지."
          "\n      **누구 것인지 물어라**: 저 상호는 누구 이름인지, 저 잡지는 누가 냈는지."
          " 그 물음이 다음 사람을 부른다.")
    for pool, head, why in (
            (SYSTEMS, "이번 대목의 제도",
             "사람을 실제로 움직이는 것은 마음이 아니라 절차다. 날짜와 번호와 금액을 대라"),
            (MEDIA, "이번 대목의 매체",
             "누가 남긴 것이 지금 여기 있다. 남긴 사람은 여기 없어도 된다"),
            (LIMITS_LIFE, "이번 대목의 제한",
             "**우연이 문제를 풀지 못하게 하는 것이 이 자리다.** 못 하는 이유가 구체적이어야"
             " 편의주의가 안 생긴다")):
        pick = shock._batch(pool, f"{seed}|{head}", n, head, PUSH_WORLD)
        lines.append(f"  · **{head}** -- {' / '.join(pick)}"
                     f"\n      {why}")
    lines.append("  · **끌어올 개념** -- 설명하지 말고 **쓰게 해라.** 인물이 그 말을 자기"
                 " 삶에 갖다 붙이는 순간 그것은 설정이 아니라 성격이 된다. 모르는 사람이"
                 " 되묻는 것도 좋다.")
    for x in shock._batch(ODD, f"{seed}|odd", n, "odd", PUSH_ODD):
        lines.append(f"      - {x}")
    for x in shock._batch(FOREIGN, f"{seed}|foreign", n, "foreign", PUSH_FOREIGN):
        lines.append(f"  · **바깥 말에서 끌어올 것** -- {x}"
                     "\n      한국어답게 다듬지 마라. **어색한 채로 두는 것이 요점이다** --"
                     " 다만 한 대목이면 된다.")
    for f in forms(seed, n):
        lines.append(f"  · **이번 대목에 섞을 꼴** -- **{f}**: {FORMS[f]}"
                     "\n      한 대목이면 된다. 원고 전체를 이걸로 쓰지 마라 --"
                     " 산문 한가운데 다른 꼴이 하나 끼는 것이 재미다.")
    if not lines:
        return ""
    return "[말맛] **쓰던 것으로만 쓰지 마라 -- 기울면 되민다.**\n" + "\n".join(lines)
