"""절단평면(cutting plane) -- **결정 하나가 최소 단위다.**

이 폴더는 VNE 논문의 시작점이고, `vne/`(재는 자)와 따로 둔다. 여기 있는 것은 발견기가
아니라 **판정기**다. 부등식이 어디서 왔든(사람·회귀·우연) 묻지 않고, 네 가지를 차례로
기계적으로 답한다.

    유효인가   max{a·z : z in X_VNE} <= b 인가        틀리면 solver 가 **틀린 최적해**를 낸다
    자르는가   max{a·z : z in P_LP}  >  b 인가        아니면 아무것도 안 자른다(버린다)
    분리하는가 a·z* > b 인가                          지금 이 fractional 점을 자르나
    조이는가   LPgap 이 줄었나                        얼마나 값어치가 있나

넷 다 **세 값**이다 -- 예 / 아니오 / **못잼**. 못잼을 예로 접는 순간 이 도구는
"valid 하지 않은 cut 을 valid 라 부르는 장치" 가 된다. 그 실패는 터지지 않는다 --
solver 가 조용히 다른 답을 낸다. 그래서 이 폴더의 기본값은 전부 '아니오'다.
"""
