requests>=2.31.0
python-dotenv>=1.0.0
langchain-core>=0.3.0
langchain-google-genai>=2.1.0
langgraph>=0.2.0
discord.py>=2.4.0

# 수학 스택(mathmetics/matrix_exponent/*)과 그 심판 verifier.py 가 쓴다.
# 없으면 G009 는 정적 검사만 하고 행동 검증(틀린 스킴을 실제로 거부하는가)을 건너뛴다.
# CI(.github/workflows/gates.yml)는 이 패키지들이 실제로 깔렸는지 먼저 확인한다 --
# 안 그러면 게이트가 조용히 SKIP 되면서 CI 만 초록으로 뜬다.
numpy>=1.24.0
# **scipy 가 여기 없어서 VM 이 깨졌다** (실측 2026-09-14).
#   numpy 2.x 는 `numpy.Inf` 를 없앴는데, 핀이 없으니 VM 에는 그것을 쓰는 옛 scipy 가
#   남아 있었다 -> `cut/ff.py` 가 import 에서 터지고 `tests/test_cut.py` 가 아예 안 돈다
#       cannot import name 'Inf' from 'numpy'
#   고치기 루프가 세 바퀴 돌고 못 풀었다(numpy 를 **내리려** 했다 -- 방향이 반대다.
#   올려야 하는 것은 scipy 다). 이 컨테이너의 실측 조합: scipy 1.17.1 + numpy 2.4.6,
#   그 scipy 의 요구가 `numpy<2.7,>=1.26.4` 다.
scipy>=1.14.0
# mathgen 의 심판이 이것으로만 푼다(mathgen/_worker.py · champion.py). 없으면
# test_mathgen_judge 가 죽는데, 예전에는 CI 목록에 그 테스트가 없어서 안 보였다.
sympy>=1.12
# 수식을 LaTeX 설치 없이 보이게 한다 (latex_formatter.py)
#   antlr4: sympy 의 parse_latex 가 쓴다 -- 없으면 유니코드 변환이 못잼이 된다
#   matplotlib: mathtext 로 PNG 를 그린다 (text.usetex=False -- LaTeX 설치 불필요)
antlr4-python3-runtime==4.11
matplotlib>=3.7

# 2026-09-02 제거: arxiv, paper-qa, pypdf, langchain-groq.
# 옵시디언 논문 리서치 파이프라인(main.py/collector.py/sources/ 등)과 함께 지워졌고
# 남은 코드 어디에서도 임포트하지 않는다. langchain 우산 패키지도 langchain-core 로
# 좁혔다 -- 실제로 쓰는 것은 langchain_core.tools 뿐이다.

# novel/drive.py 의 anthropic_llm() 이 쓴다. author(director) 역할만 Claude 로
# 돌리는 구성이라 없으면 그 경로에서만 실패한다(Gemini 경로는 무관).
anthropic>=0.40.0

# 2026-09-08 되돌림: law/ocr.py 가 시험지 PDF 에서 쪽 그림을 꺼낸다. 2026-09-02 에
# 뺐던 것과는 쓰임이 다르다 -- 그때는 논문을 읽으려던 것이었고 지금은 **글자층이 없는
# 스캔에서 그림만** 꺼낸다(글은 Gemini 비전이 읽는다). poppler 를 서버에 깔지 않으려고
# PNG 는 순수 파이썬으로 만든다.
pypdf>=5.0.0
reportlab>=4.0   # discord_pdf: 메모(.md)를 핸드폰에서 열리는 PDF 로 (2026-09-12)
