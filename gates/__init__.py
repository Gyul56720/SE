"""
승격된 게이트들 -- "메모리 행동 전이"의 종착점.

이 저장소의 에이전트는 사고를 낼 때마다 public_agent_memory/에 산문 노트를 썼다. 노트의
진단 품질은 높았지만 행동으로 옮겨지지 않았다(실측: 20:35에 "py_compile로는 부족하다,
임포트를 해봐야 한다"고 저장하고 2분 뒤인 20:37에 임포트 불가 코드를 push했다). 산문은
읽히지 않으면 아무것도 막지 못한다.

여기 있는 파일들은 그 노트들을 실행 가능한 검사로 옮긴 것이다. gatekeeper.py가 커밋 전에
전부 돌리고, 하나라도 위반되면 커밋 자체가 막힌다. 에이전트가 노트를 읽었는지 여부와
무관하게 강제된다.

새 게이트는 손으로 추가하지 않는다 -- self_challenge.py의 red-green 증명을 통과한
진단만 여기로 승격된다. Public_agent/challenge.py가 EXPECTED_SHA256으로 풀이자의
자기 확신을 배제하듯, 게이트도 "고치기 전 코드에서 실제로 실패했다"는 실행 증거를
통과해야만 들어온다.

게이트 규약:
    RULE_ID   -- "G001" 같은 고유 ID
    TITLE     -- 한 줄 설명
    ORIGIN    -- 이 게이트를 낳은 사고 커밋 해시
    EVIDENCE  -- 근거가 되는 메모리 노트 경로(있으면)
    check(ctx) -> list[str]   -- 위반 사항 목록. 빈 리스트면 통과.
"""
