# 산출물은 커밋하지 않는다 -- 원장과 같은 이유다(검사는 재는 것이지 남기는 것이 아니다).
# 보고서 PDF/HTML 과 합성 캐시는 매번 다시 만들어지고, 크고, 커밋에 들어가면
# 진짜 변경이 안 보인다.
out/
work/
ledger.jsonl
lib/nsw10.lib
lib/nsw10_dft.lib
gen/
designs.json

# 사인오프 꾸러미는 **산출물**이다 -- `python3 house/signoff.py` 가 언제든 다시 낸다.
# 넷리스트 JSON 2 MB · GDS · VCD 가 매번 바뀌므로 커밋하면 저장소만 부푼다.
# 원본(RTL · TB · SDC · UPF)은 제자리에 이미 커밋돼 있다.
signoff/
