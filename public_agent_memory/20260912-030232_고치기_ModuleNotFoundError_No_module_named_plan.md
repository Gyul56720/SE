---
topic: '고치기: ModuleNotFoundError: No module named 'plan''
---

# 고치기 루프 -- ModuleNotFoundError: No module named 'plan'

재현 명령: `python3 -m improve.run --부탁 핸드폰에서 py랑 md 파일이 안열리는데 pdf로 제공해 줄 수 있는지`

## 해 본 것
1. [명령] 명령 python3 -m improve.run --부탁 "핸드폰에서 py랑 md 파일이 안열리는데 pdf로 제공해 줄 수 있는지" -> **실패** -- 부탁 내용에 공백이나 특수문자가 포함되어 있을 때 인자가 올바르게 파싱되도록 큰따옴표로 감싸서 전달해야 합니다.
   [llm_pool] 한 바퀴가 전부 RPM/일시장애다 -- 10초 기다렸다 다시 돈다 (2/3바퀴)
2. [입력] GEMINI_API_KEY -- 제미나이 API 키(.env 또는 환경변수)가 없어 제안기를 호출하지 못했습니다. -> !열쇠 GEMINI_API_KEY=<유효한 제미나이 API 키> -> **입력오류** -- 제미나이 API 키(.env 또는 환경변수)가 없어 제안기를 호출하지 못했습니다.

## 판정
못 풀었다 (바퀴 2)

## 남은 것 (사람만 할 수 있는 한 가지)
모델은 '이용자 측' 이라 했지만 **저장소가 다르게 말한다**: 코드를 또 고치지 마라. **고침이 도착하지 않은 것이다.** 머지됐는지(scripts/pr_merged.sh) · 배포가 돌았는지 보라.
  근거: **도는 코드가 낡았다.** improve/run.py:410 의 `사용자개선` 는 지금 HEAD 가 아니라 `09ee299` (레포 전체 시뮬레이션(회귀) · `!개선 <말>` 로 사람이 말한 개선까) 의 줄번호와 맞는다. 그 커밋은 HEAD 의 조상이다 -- 고침이 그 뒤에 있다.

## 다음에 같은 증상이면
위의 해결 바퀴를 먼저 해 보고, 실패한 바퀴는 건너뛰어라.
