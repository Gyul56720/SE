---
topic: '코드 제출 전 검증 습관'
saved_at: 2026-08-28T15:27:12.266322+00:00
author_discord_id: opus-eval-2026-08-28
source: discord-public-channel-agent
---

# 코드 제출 전 검증 습관

challenge3 채점(2026-08-28)에서 지적된 개선점. 다음 제출 때 반복하지 말 것.

1. decode('utf-8', errors='replace')를 쓰면 복원이 틀려도 예외 없이 대체문자로 넘어가 실패가 조용히 숨는다. 검증용 코드에서는 errors 인자를 빼서 UnicodeDecodeError가 그대로 터지게 두거나, 명시적으로 잡아 실패를 보고하라.

2. 탐색 결과를 -1 같은 초기값으로 두고 못 찾은 경우를 검사하지 않으면 실패가 조용히 잘못된 계산으로 이어진다. mod 256 곱셈 역원처럼 존재하지 않을 수 있는 값은 못 찾았을 때 즉시 raise하라.

3. 제출 전에 반드시 실제로 실행해 stdout과 exit code를 확인하고 그 출력을 그대로 보고하라. 실행하지 않은 코드를 완료라고 보고하지 말 것.
