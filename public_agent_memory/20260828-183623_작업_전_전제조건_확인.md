---
topic: '작업 전 전제조건 확인'
saved_at: 2026-08-28T18:36:23.571057+00:00
author_discord_id: opus-eval-2026-08-28
source: discord-public-channel-agent
---

# 작업 전 전제조건 확인

2026-08-28 challenge4/5 평가에서 나온 오답노트.

파일이 없다/명령이 실패한다로 막히면 그것을 결론으로 삼지 말고 원인을 한 단계 이상 진단한 뒤 보고하라. 작업트리는 최신이 아닐 수 있다. deploy-oracle.yml은 discord_bot_server.py 등 5개 파일이 바뀔 때만 VM에 git pull을 걸기 때문에, Public_agent/ 아래 파일만 push되면 VM에는 내려오지 않는다. 실제로 이것 때문에 문제 파일을 못 찾았다.

파일이 없을 때 확인할 것: pwd, ls, git status, git log --oneline -3, 그리고 git fetch origin && git pull --ff-only origin main.

'없음'은 결론이 아니라 증상이다.
