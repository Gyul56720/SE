---
topic: '대화 간추림: investigate-248f1478'
---

# 대화 간추림 (58줄 -> 새 실)

## 사람이 시킨 것
- [조사 바퀴 20 · 남은 시간 207분] **끝까지 판다. 판정은 끝값이 한다.** 증상: TypeError: mail_body() got an unexpected keyword argument '첨부이름' 지금 
- [조사 바퀴 21 · 남은 시간 201분] **끝까지 판다. 판정은 끝값이 한다.** 증상: TypeError: mail_body() got an unexpected keyword argument '첨부이름' 지금 
- [조사 바퀴 22 · 남은 시간 196분] **끝까지 판다. 판정은 끝값이 한다.** 증상: TypeError: mail_body() got an unexpected keyword argument '첨부이름' 지금 

## 부른 도구(깃발)
- search_memory, run_shell, run_probes, read_file, run_rtl

## 실패·거절 줄
- [exit=0] STDOUT: ./tests/test_house.py:본문 = RPT.mail_body(P.SOFIA, "NSW-FIR v1.0", ./improve/ledger.
- [exit=1] STDOUT: == 수단이 없으면 딱 그것만 묻는다 == 실패 없는 것 둘 실패 안 보내고 필요한 것을 돌려준다 실패 **어떻게 주는지가 한 줄로 적혀 있다** 통
- [exit=1] STDOUT: == HEAD 판에는 커밋 안 된 것이 없다 == 통과 커밋된 파일은 보인다 (0) 통과 **커밋 안 된 파일은 안 보인다** -- precheck 
- [exit=0] STDOUT: == 수단이 없으면 딱 그것만 묻는다 == 실패 없는 것 둘 실패 안 보내고 필요한 것을 돌려준다 실패 **어떻게 주는지가 한 줄로 적혀 있다** 통
- [exit=1] STDOUT: STDERR: Traceback (most recent call last): File "<string>", line 1, in <module> Mod

## 마지막 답 머리
보고합니다. MERA-1 설계 건에 대해 `house/run.py`를 호출하여 분석을 시도하였으나, **대상 RTL 설계 파일이 특정되지 않아** 합성 및 배치(Place) 단계의 수치를 잴 수 없는 상태입니다. ### 측정 결과 * **명령**: `python3 house/run.py --설계 "MERA-1" --단계 synth` * **출력**: "회로
