# AIM 마스킹 하드웨어 -- 자동 진행 목록

사용자가 자는 동안 스스로 도는 고리가 이 파일을 읽고 쓴다. **한 번에 한 항목.**

## 규칙 (사람이 없을 때 지킨다)

- 지정 브랜치 `claude/hopeful-clarke-saauy2` 에만 민다. **PR 을 만들지 않는다.**
- `bash scripts/precheck.sh` 가 초록일 때만 커밋한다.
- **정답은 KpqC 레퍼런스가 낸다.** 검증이 스스로 정답을 짓지 않는다.
- 막히면 **멈추고 여기 적는다.** 우회해서 초록을 만들지 않는다.
- 수를 적을 때는 **무엇으로 쟀는지** 같이 적는다.
- 되돌릴 수 없는 일(강제 푸시·삭제·외부 전송)은 하지 않는다.

## 항목

- [x] **1. frob_sel** -- 배럴(7단 직렬)을 고정 선형맵 8개 + 8:1 먹스로 바꾼다.
      까닭: SYN 이 낸 최악 슬랙이 가수 -368 ns 대 이진법 -57 ns 로 5.0배였고
      그 5배가 캐스케이드 깊이다. 이 회로가 쓰는 지수는 정해진 8개(49 98 68 117
      106 84 40 80)뿐이므로 깊이를 1맵으로 줄일 수 있다.
      끝남 조건: 레퍼런스 벡터 32개 통과 + SYN 재측정 + 앞뒤 비교 기록
- [ ] **2. 파이프라인** -- Frobenius 출력을 레지스터로 끊어 곱과 다른 사이클에 둔다.
      두 모드의 주기가 같아져야 "16 대 128 = 8배" 가 정직해진다.
- [ ] **3. SDC 재설정** -- 20 ns 는 재기 전의 추측이었다. STA 가 낸 값으로 다시 정한다.
- [ ] **4. 카라추바 곱** -- 교과서 곱(AND 16384)을 줄인다. 곱이 작아지면 Frobenius 의
      상대 비중이 커지므로 1~3 의 결론이 바뀔 수 있다. 다시 잰다.
- [ ] **5. 다섯 직무 전체** -- RTL·DV·SYN·DFT·PD 를 aim_chain 에 돌려 보고서 세트를 낸다.
- [ ] **6. 변이 검사 확장** -- aim/rtl검사.sh 에 frob_sel·파이프라인 변이를 더한다.

- [ ] **7. langchain / langgraph 걷어내기** (사용자 지시 2026-09-24)
      **배포는 main 푸시에만 돈다 -- 내 브랜치 작업은 도는 봇을 안 건드린다**(확인함).

      갈아야 하는 자리 (실제 쓰임을 다 훑었다):
      | 무엇 | 어디 | 대신할 것 |
      |---|---|---|
      | `langchain_core.tools.tool` | bot_tools.py, `@tool` 약 20개 | 우리 데코레이터 (이름·설명·스키마 뽑기) |
      | `ChatGoogleGenerativeAI` | bot_tools.py:1740,1745,1750 · orchestrator/llm_pool.py:275 | `orchestrator/gemini_http.py` (이미 있다) |
      | `langgraph.prebuilt.create_react_agent` | bot_tools.py:1799 | 손으로 쓴 도구호출 고리 |
      | `langgraph.checkpoint.memory.MemorySaver` | discord_bot_server.py:234 · main_public.py:223 | thread_id 로 키 잡는 사전 |
      | `langchain_core.callbacks.BaseCallbackHandler` | bot_tools.py:34-36 | 우리 훅 (rpmgate 가 쓴다) |

      **지켜야 할 겉모습** (바꾸면 부르는 쪽이 다 깨진다):
          agent.invoke({"messages": [("user", prompt)]}, config=config) -> {"messages": [...]}
          llm.invoke(prompt) -> .content 가 있는 것
          build_agent_pool(keys, models, tools, prompt, checkpointer, fallback_models) -> [(label, agent)]

      걸음: (a) `se_agent/` 에 대체층을 짓고 검사부터 붙인다  (b) bot_tools 를 갈아끼운다
            (c) discord_bot_server·main_public 의 MemorySaver  (d) orchestrator/llm_pool
            (e) requirements.txt 에서 뺀다  (f) G012 + precheck 초록  (g) 커밋·푸시
      **agentbook/R0_langchain.py · R1_langgraph.py 는 교재다 -- 건드리지 않는다.**

## 진행 기록

- 2026-09-24 **항목 1 끝**. 배럴(7단 직렬)을 고정 선형맵 8개 + 8:1 먹스(`gf128_frob_sel.v`)로
  갈았다. 까닭은 SYN 이 낸 가수 -368.4 ns 대 이진법 -57.6 ns (5.0배)다. 레퍼런스 벡터
  32개 통과, 사이클 그대로(128 대 8). 커밋됨.
  **생성물이 뒤처지는 함정을 하나 잡았다**: `house/rtl/src/nsw_aim.sv` 를 다시 안 내서
  회사가 옛 배럴을 재고 있었다. 멈추고 다시 내고 다시 걸었다.
- 2026-09-24 **측정을 회사에 넘겼다**. 사용자 지적대로 내가 sed 로 변이를 걸고 yosys 로
  면적을 재던 것을 그만둔다. 변이는 Priya(DV), 면적·STA 는 Marcus(SYN) 의 일이다.
  `aim/run_company.py` 가 다섯 직무 x 두 설계를 돌린다.
  로그: `logs/aim_company.log` · 결과: `aim/company_out.json`
