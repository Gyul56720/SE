# 백그라운드 작업 실행 규칙

`discord_bot_server.py`가 이 세션을 `claude -p`로 실행한다. 응답을 보내고 나면 `claude -p`
프로세스 자체가 곧바로 종료되는데, 이때 Bash tool 안에서 `command &`처럼 셸 job control로만
백그라운드에 올린 프로세스는 부모와 함께 정리되어 같이 죽는다 (실측 확인됨: `python3
ai_concept_generator.py &`로 띄운 뒤 "완료되면 알려드리겠습니다"라고 답했지만, 다음 메시지가
올 때까지 아무것도 생성되지 않은 채 프로세스가 사라져 있었다).

몇 분 이상 걸리는 작업(파일 여러 개 생성, 배치 처리, 리서치 파이프라인 등)을 백그라운드로
돌려야 하면 반드시 아래 패턴을 쓸 것:

```bash
mkdir -p /home/ubuntu/SE/logs
setsid nohup <command> > /home/ubuntu/SE/logs/<작업이름>.log 2>&1 < /dev/null &
disown
echo "PID: $!"
```

- `setsid`: 새 세션을 만들어 부모 프로세스(그리고 claude -p 자체)가 종료돼도 죽지 않게 한다.
- `nohup` + `disown`: SIGHUP과 셸 job table 연결을 모두 끊는다.
- stdin/stdout/stderr를 전부 리다이렉트해서 셸이 닫혀도 안전하게 한다.
- 사용자에게 "백그라운드로 시작했다"고 답하기 전에 **살아있는지 확인부터** 하고, 확인됐을
  때만 PID와 로그 파일 경로를 같이 알려줄 것. 확인 없이 "완료되면 알려드리겠습니다"라고
  먼저 말하지 말 것.
- **확인은 `ps -p $!` 가 아니라 `pgrep -af <스크립트명>` 으로 한다.** `setsid` 는 자기가
  프로세스 그룹 리더면 fork 하므로, 셸의 `$!` 는 **이미 끝난 래퍼의 PID** 다. 실제 프로세스는
  다른 PID 를 받는다. 그래서 `ps -p $!` 는 멀쩡히 도는 작업에 대해서도 아무것도 안 찍는
  거짓 음성을 낸다(실측: `$!`=8309 는 없고 실제는 8311 이 로그를 쓰고 있었다). 죽은 줄 알고
  다시 띄우면 같은 작업이 두 벌 돌고, 같은 파일을 쓰면 서로를 덮어쓴다.
- 로그 파일에 실제로 줄이 쌓이는지도 함께 볼 것. 프로세스가 살아 있어도 첫 호출에서 죽는
  경우가 있다.
- `pkill -f <패턴>` 은 조심할 것. 명령줄에 그 패턴이 들어 있으면 **자기 셸까지 죽인다**
  (실측 확인됨). 정리할 때는 `pgrep -af` 로 PID 를 먼저 확인하고 그 PID 를 kill 할 것.
- 다음 메시지(재개된 세션)에서 진행 상황을 물어보면 추측하지 말고 로그 파일 내용과
  `ps -p <PID>`로 실제 상태를 확인한 뒤 답할 것.

# 소설: 디렉터만 Claude, 산문은 Gemini

`novel/` 파이프라인에서 **역할을 갈라 쓴다.**

| 역할 | 모델 | 왜 |
|---|---|---|
| 디렉터(주제·시나리오) | **Claude** (`claude -p`) | 호출 6회 중 1회, 출력 300토큰 남짓. 무엇을 쓸 것인가는 전부 여기서 갈린다 |
| 배우 · 화자 · 추출기 | **Gemini** | 토큰의 대부분이 여기다. 산문과 배우 턴이 회차당 100호출 중 대부분을 차지한다 |

`scripts/run_novel_vm.sh` 의 기본값이 이 배치다(디렉터 실패가 3회 이어지면 Gemini 로
자동 강등, 뒤에 탐침 한 번으로 복귀).

- **`--all-claude` 를 쓰지 말 것.** 산문까지 구독으로 청구된다. 실측: 1화(3씬)에 20분과
  상당한 토큰. 같은 일을 Gemini 는 API 쿼터로 처리한다.
- 여기(에이전트 세션)에서 **직접 산문을 지어내 보여주는 것도 하지 않는다.** 같은 낭비이고,
  파이프라인이 만든 것이 아니므로 결과를 오해하게 만든다. 문체를 바꾸고 싶으면
  `novel/style.py` 의 페르소나를 고쳐서 Gemini 가 그렇게 쓰게 한다.
- Gemini 키가 없으면 사실대로 "못 돌린다" 고 답한다. Claude 로 대신하지 않는다.
- 예외는 **배선 확인**뿐이다. 가짜 LLM(tests/) 이나 `--world probe` 로 호출 경로를
  점검하는 것은 산문 생성이 아니므로 상관없다.

# 머지하지 않은 PR 의 명령을 주지 마라

**실측 2026-09-08, 같은 날 다섯 번**: PR #57 · #62 · #64 · #66 · #67. 다섯 번 다
"PR 에 담았습니다. `git pull origin main` 하고 이걸 돌려 보세요" 라고 답했는데
**그 PR 이 머지돼 있지 않았다.** 사용자는 다섯 번 다 바뀌지 않은 숫자를 보았고,
그 숫자를 근거로 잘못된 판단을 했다:

    #57  놓침 14 가 그대로 -> "개선되고 있다" 고 읽음 (실은 같은 수)
    #62  `law/bench.py` 없음 -> "파일이 없다" 는 오류
    #64  `--판례` 없음 -> "옵션이 없다" 는 오류
    #66  문 64 가 그대로 -> 거짓 양성이 안 고쳐진 채 남음
    #67  `--이력` 없음 -> "옵션이 존재하지 않는다" 는 오류

**한 번은 실수지만 다섯 번은 절차의 결손이다.** 그래서 순서를 뒤집는다.

## 규칙

1. **머지가 끝나기 전에는 그 변경을 쓰는 명령을 한 줄도 주지 않는다.**
   그런데 **CI 를 기다리느라 미루지도 않는다** -- 아래 '## CI 를 기다리지 않는다'.
   밀고 · PR 내고 · 바로 머지하고 · 그 답에서 명령을 준다. 한 답에 다 한다.
2. 명령을 주기 직전에 **머지됐는지 실제로 확인한다** -- 기억이 아니라 조회로.

       scripts/pr_merged.sh 67
         0  머지됨      -- 명령을 줘도 된다
         1  머지 안 됨  -- 명령을 주지 마라
         2  모르겠다    -- 조회 실패. **모르는 것은 안 된 것으로 다룬다**

   이 장치의 첫 판이 거짓 음성을 냈다(깃허브가 `"merged": true` 처럼 공백을 넣는데
   `"merged":true` 로만 찾았다). **잘못 답하는 장치는 없느니만 못하다** -- 통과했다는
   이유로 더 마음 놓고 명령을 주게 된다. 그래서 `tests/test_pr_merged.py` 가 그 자리를
   붙들고 있다.
3. 사용자가 보낸 결과가 **직전과 똑같으면 먼저 머지 여부부터 의심한다.**
   숫자가 안 변한 것은 대개 코드가 안 바뀐 것이지 변경이 무력한 것이 아니다.

## 왜 이 순서인가

이 저장소의 규율은 **검사하지 않은 초록불이 검사한 빨간불보다 나쁘다** 이다.
머지 안 된 PR 을 두고 "돌려 보세요" 라고 하는 것은 정확히 그 꼴이다 --
사용자는 초록불(내 보고)을 보지만 아무도 그것을 검사하지 않았다.

# CI 를 기다리지 않는다

**사용자 지시(2026-09-08): "CI skip 하고 그냥 다 pull request 하게".**
CI 초록불을 기다리느라 답이 한 바퀴씩 늦어졌다. 기다림을 없앤다.

    쓴다 -> `bash scripts/precheck.sh` -> 밀기 -> PR -> **바로 머지** -> 명령을 준다

한 답에 다 한다. "CI 가 돕니다" 로 답을 끊지 않는다.

## 그래도 재고 나서 민다

이 저장소의 규율은 **검사하지 않은 초록불이 검사한 빨간불보다 나쁘다** 이다.
CI 를 안 기다리는 것은 *검사를 안 하는 것*이 아니라 *검사를 여기서 하는 것*이다.
CI 가 돌리던 것과 같은 `scripts/tests.sh` 를 밀기 전에 내가 돌린다.

**CI 가 혼자 잡아 주던 것이 딱 하나 있다 -- 커밋에 안 담긴 파일.** `git add` 를
빠뜨려도 내 작업 디렉터리에는 그 파일이 있으니 검사가 초록불을 낸다. 그런데 남이
받아 가는 것은 커밋된 나무뿐이다. 그러면 사용자가 "그런 파일 없다" 를 보고, 그것이
이 저장소가 다섯 번 앓은 바로 그 병이다(#62 `law/bench.py` 없음).

그래서 `scripts/precheck.sh` 가 **HEAD 를 임시 워크트리로 꺼내 거기서** 검사를
돌린다. 작업 디렉터리를 안 본다. 빨간불이면 밀지 않는다.

**기본은 빠른 길(law 검사)이다.** 전체 검사는 6분 넘게 걸리는데, 그것을 여기서
기다리면 CI 를 기다리던 것과 똑같아진다 -- 없애려던 바로 그 기다림이다. 나머지는
CI 가 뒤늦게 알려 주게 두고, 필요하면 `--전부` 로 여기서 다 돌린다.

## 남은 빨간불은 그대로 말한다

`tests/test_mathdrift_prove.py` 가 `origin/main` 에서 이미 실패하고 있다. 이건
내 변경 탓이 아니고 수학 쪽이라 안 건드린다. **다만 초록불이라고 말하지 않는다** --
몇 개가 왜 실패하는지 매번 그대로 적는다. CI 를 안 보게 되었으므로 이 보고가
사용자가 그것을 아는 유일한 통로다.

CI 워크플로 자체는 그대로 둔다(끄지 않았다). 뒤늦게 오는 신호는 공짜이므로 버릴
이유가 없고, 다만 **그것을 기다리지 않을 뿐**이다. 워크플로까지 끄기를 원하면
`.github/workflows/gates.yml` 의 `pull_request` 를 빼면 된다 -- 한 줄이다.


# 남이 같이 쓰는 브랜치에서는 rebase 하지 마라 -- merge 해라

**실측 2026-09-08:** `claude/light-novel-dopamine-elements-w6gtdg` 로 밀다가

    ! [rejected] ... (non-fast-forward)
    hint: the tip of your current branch is behind its remote counterpart

**rebase 를 하고 나서도 계속 실패했다.** 그럴 수밖에 없다 -- rebase 는 내 커밋을 새로
쓰는 것이라, 원격이 움직인 뒤에는 fast-forward 가 **영영 안 된다.**

## 왜 원격이 움직이나

이 저장소의 브랜치에는 **봇이 같이 쓴다.**

    SE-agent: Discord 요청 처리 결과 자동 반영

`w6gtdg` 브랜치에만 그런 커밋이 열 개가 넘고, 하나는 그 세션이 일하는 동안 들어왔다.
그래서 내가 붙들고 있는 동안에도 원격 끝이 앞으로 간다.

**이미 네 번 같은 일이 났고 네 번 다 merge 로 풀었다** -- `2c1bafa` · `847935d` ·
`9a953a4` · `6f3f985`. 다섯 번째는 절차의 결손이다.

## 규칙

1. **`main` 이 아닌 브랜치에서는 `git merge origin/<그 브랜치>` 를 쓴다.** rebase 는
   나 혼자 쓰는 브랜치에서만.
2. **`git rebase origin/main` 을 아무 브랜치에나 걸지 마라.** 밑동이 바뀌어 갈라진다.
3. **`--force` / `--force-with-lease` 를 쓰지 마라.** 봇 커밋과 다른 세션의 일이 통째로
   사라진다. 밀리는 것은 고칠 수 있어도 지워진 것은 못 되돌린다.

## 막혔을 때

```bash
B=$(git rev-parse --abbrev-ref HEAD)
git fetch origin "$B"
git log --oneline origin/"$B"..HEAD      # 내가 새로 만든 것만
git merge origin/"$B"                    # rebase 가 아니다
git push -u origin "$B"
```

이미 rebase 를 해서 같은 커밋이 두 벌 생겼으면, 원격을 진실로 삼고 내 것만 얹는다.

```bash
git branch backup/rebase-사고            # 지금 것을 남긴다. 지우지 않는다
git fetch origin "$B"
git log --oneline origin/"$B"..HEAD      # 이 목록이 '내 것'
git reset --hard origin/"$B"
git cherry-pick <위 목록, 오래된 것부터>
git push -u origin "$B"
```
