# -*- coding: utf-8 -*-
"""SE 실습 흐름 -- RTL 에서 테스터까지, 이 저장소 안에서 전부 돈다.

## 왜 직접 짓나

이 컨테이너에는 yosys · iverilog · verilator · ngspice 가 있고 **OpenROAD ·
klayout · magic 은 없다.**  없는 도구를 깔라고 사람에게 시키지 않는다(저장소
규칙: "사람에게 설치를 시키지 마라").  그래서 배치·CTS·배선·사인오프는
**파이썬으로 직접 짓는다.**

그리고 그게 오히려 낫다.  상용 도구를 부르면 안이 안 보여서 "돌렸다" 는 것
말고는 잴 것이 없다.  직접 지으면 단계마다 **수가 나오고 그 수를 검사가 다시
잰다** -- 이 저장소가 교재에서 지키는 규율을 실습에서도 지킬 수 있다.

## 단계

    01 합성      yosys        RTL -> 게이트 넷리스트 (진짜 도구)
    02 STA       se/sta       도착·요구·슬랙, 임계경로
    03 플로어플랜 se/floorplan 코어 치수, 파워링·스트라이프, IR
    04 배치      se/place     힘방향 + 담금질, 합법화, HPWL
    05 CTS       se/cts       클럭트리, 삽입지연·스큐, OCV
    06 배선      se/route     전역(gcell) -> 상세(미로) -> 탐색수리
    07 사인오프  se/signoff   기생 재추출 -> 재STA, 밀도
    08 DFT       se/dft       스캔 삽입, 고장 시뮬, ATPG, 커버리지
    09 DV        se/dv        UVM 꼴 테스트벤치 + 커버리지 + 변이검사

    `python3 lab/run.py` 가 전부 돌린다.
"""
